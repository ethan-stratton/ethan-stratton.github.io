# 2026-04-06

## Iron & Ether — Session Notes

### Camera Scrolling Fixes (Room 11)
- **Light mask ambient cutoff** (`15a0e8b`): Ambient fill in light mask RT used fixed world coords — scrolled off-screen when camera moved. Fixed by offsetting by `_cameraOffset.Y`.
- **Vignette center** (`392538f`): Both shader and CPU vignette paths used raw `_playerPos / screenSize`. In tall rooms, vy went >1.0, pushing vignette off-screen → uniform blue tint. Fixed: `(_playerPos - _cameraOffset) / screenSize`.
- **CPU vignette offset**: `vigOffsetY` used `(_playerPos.Y - 360f) * 0.3f` — same bug. Fixed with `- _cameraOffset.Y`.
- **HUD draw split** (`392538f`): Pickups, ether drops, tripwires were in untransformed SpriteBatch but use world coords. Split into camera-transformed batch (world items) + untransformed batch (HUD/textbox/room name).

### 4 New Triple Combos Implemented (`cc3142c`)
- **Za Hando** (Lance+Linger+Dark, 35 EP, 2.5s cooldown): Dimensional rift at beam endpoint. Gravity pull 200px radius. Scrolling green/blue/black vesica piscis tear visual. 4s linger. Stored beam direction in `VineTimer`/`GrowRate` fields for tear orientation.
- **Mollificare** (Bolt+Possess+Sap, 20 EP): Pacification bolt → slow zone on hit (15% speed via `SlowAmount`). Concentric expanding ripple visuals. 5s linger. Custom FireWeapon section (returns early, before Shadow Stitch).
- **Abhiseka** (Lance+Bloom+Hollow, 30 EP): Hallowed ground at beam endpoint. Tick damage + heals player 2 HP/tick. White fire ascending pillars + ritual circle markings. 6s linger.
- **Sambhogakaya** (Orbit+Bloom+Hollow, 35 EP, 3.0s cooldown): Self-centered transcendence. 2.5s invuln (`_iFrameTimer`) + AoE silence + tick damage. Three tilting orbital rings (gold/white/violet) that tilt opposite direction over lifetime. 4s linger. Zone follows player (`z.Position = _playerPos` in update tick).

### Build Gotchas
- Enemy struct uses `Hp` not `Health`, `SlowAmount` not `SlowFactor`
- Player HP: `_playerHp` / `PlayerMaxHp` (const 100f)
- `MathF.Lerp` doesn't exist in this .NET version — use `MathHelper.Lerp`
- Switch expressions inside object initializers: `Color = sp % 3 switch {...}` fails because parser sees `Color = sp` then `% 3`. Fix: `(sp % 3) switch {...}`
- Loop variable `i` inside orbit section of FireWeapon conflicts with outer `i` — rename to `sp`

### Quick-Fire EP Costs
- Added quick-fire costs for all 4 new combos (×1.5 penalty applied separately)
- Quick-fire fire timer overrides: Za Hando 2.5s, Sambhogakaya 3.0s

### Naming Origins (from Ethan)
- Za Hando = JoJo reference (The Hand stand that erases space)
- Mollificare = Latin "to ease, soothe, make less angry"
- Abhiseka = Sanskrit consecration/anointing ritual
- Sambhogakaya = Buddhist "enjoyment body" — transcendent/divine form

## Combo Polish Pass (commits `e6b7af3`, `e8c3acb`)

### Za Hando Redesign
- User: "should replace the purple beam, it becomes the purple beam projectile. nothing else"
- Removed linger zone + gravity pull entirely. Za Hando IS the beam — thick (10px), green, 2× damage, 0.6s lifetime
- Beam color overridden to `EssenceColor(0, 200, 120)` (rift green)

### Mollificare Redesign
- User: "needs another more unique effect... maybe it pacifies them permanently until you attack them, but they can't die?"
- Added `Pacified` bool to Enemy struct. Pacified enemies: stop attacking (`return` before AI), can't die (HP floors at 1), milky white tint + halo
- Any non-Mollificare projectile hit un-pacifies them
- "PACIFIED" floating text on hit
- Sambhogakaya does NOT un-pacify (user said "this could be kinda cool to leave in")

### Abhiseka Polish
- Added small ritual circle particle burst at player's feet when fired (16 ascending light particles)

### Sambhogakaya Fixes
- Zone position update moved from inside tick timer (every 0.4s) to every frame — now follows player smoothly
- Duration reduced from 4s to 2.5s to match invulnerability window
- Silence indicator: red pixel-art X drawn above silenced enemies (replaces tiny 3px pip)

### Wellspring Heart Relic
- New relic enabling passive EP regen (`_hasEtherRegen`). Symbol ∞, teal color
- Auto-granted on awakening start for testing
- User needed it because "I have a hard time testing these because I always run out of EP"

### Bug Fixes
- **Cursor aim in scrolling rooms**: `FireWeapon` received raw `mouse.X/Y` (screen coords) instead of `_mouseWorld` (world coords with `_cameraOffset`). All 3 call sites fixed via sed
- **Room 11 arena size persistence**: `InitAwakeningRoom5()` didn't reset `_arena` — reloading after Room 11 kept tall 1280×1440 arena. Fixed: reset to standard 1160×580 + `_cameraOffset = Vector2.Zero`

## Infrastructure Notes
- **Forbidden Laboratory**: Next.js site at `workspace/forbidden-laboratory`, runs on port 3000. No git remote. Lost when container restarts — needs cron keepalive
- **Iron & Ether has NO GitHub remote** — repo is local only. Ethan wants to share it with someone, asked about GitHub. Needs to set up remote + push
- Ethan may not have a GitHub account — need to ask

## GitHub Setup (05:00 UTC)
- **Repo created**: `https://github.com/ethan-stratton/iron-and-ether` (private)
- GitHub username: `ethan-stratton`
- Classic PAT: `[REDACTED-PAT]` (repo scope)
- Fine-grained PAT had insufficient permissions — classic token with `repo` scope worked
- Old repo had 3.3GB git history (WAV music packs + bin/obj tracked). OOM on `git gc --aggressive`
- **Solution**: Created fresh repo (`iron-and-ether-fresh`) with only source files, swapped it in as main
- `.gitignore` excludes: `*.wav`, `*.mp3`, `**/bin/`, `**/obj/`, music source packs (`Boss Battle Music Pack Vol*`, `Fantasy RPG Music Vol*`)
- `.ogg` files (game music, ~31MB total) ARE tracked — needed for build
- `iron-and-ether-old` dir still exists with full history + audio assets
- **Workflow agreed**: I commit locally → Ethan tests → on "it works" I push. No auto-push without verification
- Ethan's Mac path: `/Users/ethanstratton/Documents/OpenClaw/OpenClaw/workspace/iron-and-ether-old/IronAndEther/`
- He runs `git pull` then `dotnet run` to get changes

## Pacified Enemy Behavior (`35ab5e8`, `2664c9d`)
- Enemies with `Pacified = true` now wander instead of standing still
- Wander speed 18 px/s, direction changes every 2-4s (hash-based RNG using `Id * 7919 + AiPhase * 104729`)
- Languid deceleration: `Velocity *= (1f - 0.3f * dt)` between direction changes
- Wall bounce/clamp at arena edges
- Visual sway: `MathF.Sin(totalTime * 1.5f + Id * 2.3f) * 2f` horizontal + `MathF.Sin(totalTime * 2f + Id * 1.7f) * 1.5f` vertical
- Added `float totalTime = 0f` parameter to `Enemy.Draw()`, passed from Game1 as `_totalTime`
- **No player avoidance** — Ethan wanted them to move freely regardless of player position
- Future plan: pacified humanoid enemies will have dialogue/voice lines when player interacts

## Forbidden Laboratory Fix
- `/combos` page returned 500 — Next.js 16 canary static export bug with client components
- `.next/server/app/combos/page.js` missing from production build (Turbopack outputs `combos.html` flat)
- Fix: switched to `next dev` mode instead of `next start`
- Old `next-server` (pid 11371) was holding port 3000 — had to `kill` it before restart

### Ethan's Mac Directory Fix (afternoon)
- Ethan renamed `iron-and-ether-old` → `iron-and-ether-backup`, moved existing `iron-and-ether` → `iron-and-ether-old2`
- Fresh `git clone` succeeded at `/Users/ethanstratton/Documents/OpenClaw/OpenClaw/workspace/iron-and-ether/`
- Workflow confirmed: `cd iron-and-ether/IronAndEther && git pull && dotnet run`
- Music needs manual copy from backup: `Boss Battle Music Pack Vol. 2` and `Fantasy RPG Music Vol. 2`

### Shipability Assessment
- Gave Ethan a full breakdown of what's needed to ship across graphics, lore, story, gameplay, audio, polish
- Estimated 6-12 months total. Recommended vertical slice first (Ashlands zone)
- Art direction is the biggest blocker — suggested commissioning pixel artist for vertical slice ($500-1500)

### Story Recap & Act 1 Design
- Ethan couldn't remember the story — walked him through the full Story Bible and Vision doc
- **Decision: Gameplay-first, story as texture** (Hollow Knight model, not Planescape)
- Designed full Act 1 "The False Savior" with 3 zones:
  - Zone 1: The Ashlands (15 screens, tutorial, 3 quests, boss drops Shatter)
  - Zone 2: Whispering Swamp (20 screens, first anchor, Palamedes intro, 5 quests)
  - Zone 3: Drowned Valley (20 screens, second anchor, Dinadan intro, 5 quests)
- 13 total Act 1 quests designed with givers, rewards, and types
- Screen ratio rule: 40-50% combat, 20-25% exploration, 15-20% transition, 10-15% NPC, 5% boss

### Gameplay Loop Balancing Discussion
- Ethan worried about fast twin-stick combat inside methodical Zelda exploration structure
- Identified 5 reference games: Hyper Light Drifter, Hades, Enter the Gungeon, **CrossCode** (closest match), Death's Door
- Key design principles agreed:
  1. Transition screens between combat and calm prevent whiplash
  2. 40-50% combat screen ratio max
  3. **Gun interacts with world** (CrossCode lesson) — essences double as exploration tools
  4. Fusion tinkering IS the built-in breathing room
  5. Small arenas — single-screen format is a feature
- Recommended Ethan play CrossCode
- Formula: "CrossCode meets Hyper Light Drifter with Aria of Sorrow's collection hook"

### Evening Session — Sword, Tile Renderer, Tile Painter

**Sword mechanic added (Shift key)**
- Left Shift swings toward cursor, 90° arc, 40px range, 0.25s cooldown (spammable like ALttP)
- 6 dmg, 120px knockback, 15 stagger, sweep visual (blade line + trailing arcs + tip spark)
- No lunge (removed per Ethan), zero EP cost
- Commits: `e1b1c59`, `7ff31e5`

**Tile Renderer game mode** (`7d03cb5`)
- Browse mode: view 5 OpenRPG sheets, WASD pan, +/- scale, Tab cycles, hover shows tile ID

**Tile Painter** (`6fca5d9`, `d7e37dc`)
- P toggles browse↔paint; palette on left, room grid on right
- WASD pans (Room 11 = 25×28, others = 25×13); Ctrl+S saves `Content/room_tiles.json`
- Painted rooms override auto-tiled floors in gameplay

**Auto-tiled floor fallback** (`b53d761`)
- FloorStyle enum per room; Ethan noted tiles looked boring, rooms 1/3 "jacked up"
- Painter solves this — Ethan paints rooms himself

**Control scheme settled:**
- WASD=move, Space+dir=dash, Space alone=jump, Shift=sword, LClick=wand, RClick=alt-fire, Q=loadout swap, E=utility, Tab=inventory

### Tile Painter — Late Session Improvements (22:00-22:45 UTC)

**System.Text.Json migration** (`41d1f2f`)
- Hand-rolled JSON serializer was broken — tiles reverted on reload
- Replaced with `System.Text.Json` for reliable round-trip

**Full-screen tile grid** (`41d1f2f`)
- Expanded paint grid from arena-only to full screen (27×15 normal, 27×30 Room 11)
- Walls/borders now paintable cells; painted rooms skip flat wall rects

**Collision overlay** (`998b080`)
- G key toggles overlay showing: arena bounds (green), wall rects (red), cave area (blue), doors (magenta), torches (yellow)
- `InitScreenWalls()` called on entering paint mode and room switch

**Two tile layers — BG + FG** (`023f70c`)
- L key toggles active layer (BG=floor, FG=overlay/walls/trees)
- FG renders on top of BG with transparency
- Both layers saved in `room_tiles.json` under `{"rooms":{"5":{"bg":[...],"fg":[...]}}}`
- Legacy single-layer format loads as BG

**Transparency key fix** (`01b6f67`)
- RPG Maker 2K/3 chipset format: top-left pixel = transparency color
- Actual key is (60,143,75) green, NOT (255,0,255) pink
- Now reads top-left pixel per sheet dynamically

**Cave collision repositioned** (`01b6f67`)
- Wall rects enlarged to tile-sized (48px) instead of thin rects
- Entrance hitbox = 48×48 at (ax+316, ay+196)
- Still needs verification from Ethan — positioning estimated from screenshot

### Open from this session
- Cave collision may need further adjustment once Ethan tests
- Tile rotation not yet implemented (flip H/V + 90° rotation per cell)
- Ethan wants ability to move invisible collision tiles in editor (future)
- Torch/liquid/interactable placement in editor = Phase 1 Step 2
