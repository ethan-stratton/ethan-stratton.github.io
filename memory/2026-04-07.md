# 2026-04-07

## Iron & Ether — Tile Editor & Polish Session (continued from 04-06)

### Commits
- `20844b4` — Cave collision nudge, hide borders for tiled rooms
- `0950494` — Fix IndexOutOfRange crash in tile painter for Room 11 scrolling (bounds check on array access)
- `0b46ebb` — Cave collision fine-tune: walls +1x/-2y, entrance +8x/-2y, pillar +4x
- `188e851` — Cursor tile preview (semi-transparent ghost), door overlay fix (InitAwakeningDoors on paint mode enter)
- `8e8cb57` — Player size 16→20, FG tiles over player (tree canopy layering), jump light shrink, cave wall collision skipped when _inCave, wand pickup repositioned, screen transition edge
- `bb6bfeb` — Room 5b (cave interior, room ID 50) in tile editor, cave entrance/exit ALttP-style animation (0.6s, shrink/grow + fade), FG tile layer split from BG

### Key Changes
- **PlayerSize 16→20** — slightly larger character
- **FG overlay tiles draw AFTER player** — `DrawTiledFloorFG()` called after `DrawPlayer()`, tree canopies obscure player
- **Cave entrance animation**: entering = walk toward cave + shrink + fade to black; exiting = immediately switch to overworld + grow from cave entrance
- **Room 50 = "5b (Cave)"** in editor — 9×7 tile grid, cycles between Room 5 and Room 6
- **Cave interior centered on screen**: `caveCX = ScreenW/2, caveCY = ScreenH/2` (was offset to overworld position)
- **GetPaintArena()** helper for editor arena sizing per room
- **paintRooms array** for room cycling: {1,2,3,4,5,50,6,7,8,9,11}
- **Pause screen retains lighting** — light mask and vignette now render during GameState.Paused
- **Door visuals moved back 1 tile** (±TSDst offset per side)
- **Wall collision skipped when _inCave** — fixes wand being unreachable due to overworld wall rects
- **Wand pickup at cave center** (ScreenW/2, ScreenH/2 - 10)

### Pending (not yet committed)
- Door visual positions shifted by TSDst (in uncommitted build)
- Cave area centered on screen (in uncommitted build)
- Need to commit + push after verifying build

### Open Issues
- Cave collision positions may still need adjustment after centering change
- Sword build error still exists (StaggerAmount/StaggerThreshold → Stagger/MaxStagger)
- Screen transition edge kept at PlayerSize (halving broke doors — player clamp was stricter than trigger)
- Cave entrance drawing hidden when room has painted tiles (DrawCaveEntrance skipped)

### Design Decisions
- FG layer = "over player" layer (tree canopies, foreground decoration)
- BG layer = "under player" layer (floors, walls, background)
- Cave is a sub-room of Room 5, not a separate screen transition
- Room cycling in editor uses explicit array, not min/max range
- Jump shrinks player light radius: 250 - jumpHeight*1.5, min 120

## Session 2 — Transitions, Collision Editor, Polish (~00:20-00:52 UTC)

### Commits
- `975faf4` — Screen-edge transitions: player clamp + transition triggers use full screen bounds (0→1280, 0→720) instead of arena rect (60,80,1160,580)
- `270eba2` — P crash fix (disabled during Playing/Paused), vignette scales with ambient (40% at day, 100% at night), T menu layout fix, door overlay at screen edges
- `965d5d2` — Collision editor: C key in tile painter toggles collision mode

### Key Changes
- **Transitions now at screen edges** — `transL/R/T/B` = `PlayerSize` from screen border. Player clamp also expanded to full screen bounds. Eliminates 60px dead zone on sides.
- **P key crash fixed** — toggling post-process during gameplay left render target active → `Cannot call Present`. Now P disabled during Playing/Paused states.
- **Vignette scales with ambient light** — `vigScale = 1 - _ambientLight * 0.6f`. At full daylight, vignette is only 40% strength. Full at night.
- **T menu layout** — bloom/vignette line split in two, background height 300→360, starts 5px higher
- **Door overlay in editor** — positions moved to screen edges (`gridX + ScreenW`, etc.) matching new transition system
- **Collision editor** — C key toggles mode in tile painter. Left-drag creates wall rects (grid-snapped), right-click deletes. Red fill + size labels. Yellow drag preview. Auto-saves to `room_tiles.json` collision array. Editor collision overrides hardcoded `InitScreenWalls`.

### New Fields
- `_roomCollision` — `Dictionary<int, List<Rectangle>>` per-room collision rects
- `_trCollisionMode`, `_trDraggingCollision`, `_trCollDragStart`, `_trCollDragIdx`, `_trCollHoverIdx` — editor state

### Data Format Change
- `room_tiles.json` now includes `"collision":[[x,y,w,h],...]` per room alongside `bg`/`fg` arrays

### Ethan Feedback
- Wants collision editing before room design (good call — avoids manual tweaking)
- Noted torch tiles in tileset (IDs 264, 265, 293, 318, 381) — wants auto-lighting + Cinder utility interaction
- Custom tiles deferred ("cross that bridge when we need to")
- Vignette too dark at 100% brightness daytime — fixed with ambient scaling

## Late-night session (~01:00-04:00 UTC)

### Torch Auto-Lighting System
- **Commits**: `999e6fc`, `016636f`, `e97db94`, `2188f78`, `8e31a9b`
- `AddTorchesFromTiles(int room)` scans BG+FG layers for light-emitting tile IDs
- Uses `(sheet, tile)` pairs — sheet-aware matching prevents false positives (e.g. exterior grass ≠ dungeon torch)
- **Correct light-emitting tile IDs** (Ethan corrected earlier wrong numbers):
  - Sheet 0 (dungeon): 243,245,273-275,303-305,333-335 (lava), 263,264 (torches), 207-209 (flames)
  - Sheet 1 (exterior): 318, 381
  - Sheet 2 (interior): 124,154,184,214 (candles), 125,155,185,215 (fireplace)
  - Sheet 3 (ship): 124,154,184,214 (ship lanterns)
- **Bug**: `InitAwakeningRoom5()` has its own init path separate from `InitAwakeningRoomContent()` — torch scanning wasn't being called on startup. Fixed by adding `AddTorchesFromTiles(5)` at end of Room 5 init.
- **Bug**: `InitAwakeningRoom4()` has hardcoded torches that run AFTER the main torch setup — override tile data. Fixed with `AddTorchesFromTiles(4)`.
- If room has tile data, `AddTorchesFromTiles` clears hardcoded torches and replaces with tile-scanned positions.

### Cave (Room 5b) Fixes
- **Commit `14bf171`**: Torches/tiles from Room 5 were bleeding into cave. Fixed: skip BG+FG tile rendering when `_inCave`. Clear `_torches` on cave entry, restore via `AddTorchesFromTiles(5)` on exit. Cave gets centered light + scans room 50 tile data.

### One-Way Collision System
- **Commit `032598f`**: `CollRect` struct replaces `Rectangle` in `_roomCollision` — has `OneWay` field (0=solid, 1-4=direction player CAN pass FROM)
- Hold arrow key while releasing drag to set one-way direction
- Blue tint in editor (vs red for solid), direction label shown
- Projectiles pass through ALL one-way walls (no bullet bouncing off ledges)
- `_screenWallOneWay` parallel dict alongside `_screenWalls` for gameplay
- **Commit `1bf51eb`**: Mini-hop (50% JumpStrength) triggered when crossing one-way walls

### Layer Visibility Toggle
- **Commit `032598f`**: V key cycles: Both → BG only → FG only → Both. Header shows `[BG ONLY]`/`[FG ONLY]`.

### Player Rect
- **Commit `032598f`**: Replaced knight sprite with simple colored rect showing exact collision size + eyes for facing. Original knight preserved in multi-line comment block.

### Data Format
- `room_tiles.json` collision arrays now store 5 ints: `[x, y, w, h, oneWay]` (backward compatible — missing 5th = 0)

### Design Decisions
- Ground detail tiles (transparent FG tiles like leaves) deferred — can't go on BG because they're transparent overlays. Need a mid-layer eventually.
- Cave editor issues (5b borders, altar collision, exit collision) deferred — hardcoded special objects, not worth touching now.
- FG layer reserved for overhead-only (canopies, vines). Ground details need future solution.

## Afternoon session (~04:00-14:15 UTC)

### Sword Sprite Fix
- **Commit `4f840e7`**: Sword was invisible — draw code trapped inside commented-out `/* ORIGINAL KNIGHT DESIGN */` block after a `return;`. Moved sprite draw before the return.
- Uses top-left 16×16 from `Exotic Swords.png` (48×48 sheet, 3×3 grid). 3x scale, 3 trailing afterimages.
- **Commit `965509d`**: Pivot moved to base/hilt (origin 8,15), distance reduced 20→8px to attach to player body.

### V-Key Layer Erase Fix
- **Commit `4f840e7`**: Right-click erase now respects V-key view mode (BG only → erases BG, FG only → erases FG, both → uses active layer from L key).

### ALttP-Style Ledge Hops (One-Way Wall Rework)
- **Commit `3504f11`**: Complete rewrite of one-way wall behavior.
- **Reversed convention**: Arrow direction = direction player CAN HOP THROUGH (Down = jump down off ledge). Was backwards before.
- **Hold mechanic**: Hold WASD into ledge wall for 0.15s → triggers hop. No accidental passes.
- **Hop animation**: 0.35s smoothstep movement with parabolic jump arc (30px peak). Landing dust particles.
- **Player locked during hop**: movement input disabled, wall collision skipped.
- Fields: `_ledgeHopping`, `_ledgeHopTimer`, `_ledgeHopTarget`, `_ledgeHopStart`, `_ledgeHoldTimer`, `_ledgeHoldDir`
- Constants: `LedgeHopDuration=0.35f`, `LedgeHopHoldTime=0.15f`
- Editor labels updated: `↑HOP UP`, `↓HOP DOWN`, `←HOP LEFT`, `→HOP RIGHT`

### Bullet One-Way Fix
- **Commit `965509d`**: Bullets only pass through ledge walls when traveling in the hop direction. Wrong direction = bullet hits wall normally. Was passing through both sides before.

### Sword Build Error — Already Fixed
- `StaggerAmount`/`StaggerThreshold` bug was already resolved in a prior session. Sword uses correct `e.Stagger`/`e.MaxStagger` fields.

### Step 3: Loadout Quickswap + Utility Slot
- **Commit `b12cf00`**: Full implementation.
- **Data**: `_loadoutSecondary` (Loadout struct), `_utilityEssence` (Essence enum), `_loadoutIsPrimary`, `_loadoutSwapTimer`
- **Q key**: Swaps primary ↔ secondary loadout. White flash overlay. `RefreshCombo()` called after swap.
- **HUD**: `[P]`/`[S]` indicator (gold/blue), secondary preview below primary (dimmed), utility slot on right (`E:UTILITY`)
- **TAB menu**: Secondary loadout section (blue highlights) + utility essence section (green highlights) below primary wand config
- **Layout constants**: `InvSecHeaderY`, `InvSecFormY`, `InvSecBehY`, `InvSecEssY`, `InvUtilLabelY`, `InvUtilItemsY`
- **GetEssenceColor()**: Static helper for consistent essence coloring across HUD
- **E key use action NOT yet implemented** — that's Step 4 (environmental interactions)
- Utility costs no EP (design decision from earlier session)

### Phase Progress Assessment
- **Step 1 (Tileset Renderer)**: COMPLETE
- **Step 2 (Room Data)**: Mostly complete (grid + transitions + collision). Missing general interactable state persistence.
- **Step 3 (Loadout Quickswap + Utility)**: COMPLETE (minus E key environmental use = Step 4)
- **Step 4 (Environmental Interactions)**: Not started
- Next: Step 4 → Cinder lights torches, sword breaks walls, Bone puzzles

## Evening Session (~19:50-20:28 UTC)

### Commits
- `4616c79` — Ashlands tileset wired up (sheet index 5) + half-tile collisions (Shift key = 24px snap)
- `7cdab89` — Collision cursor: crosshair + snapped cell highlight + ½ label
- `d368947` — Morien player sprite from `knights_1x.png` (3rd knight, dark visor) + jump height halved (280→140)
- `68c0747` — Walk cycle ping-pong (0-1-2-1) instead of loop (0-1-2-0)
- `72ee6f1` — Ledge walls now non-blocking trigger zones + arrow keys disabled for room switch in collision mode

### Key Decisions
- **Ashlands tileset**: `tf_ashlands/ashlands_tileset.png` (640×336, 16×16 grid, 40×21 tiles) — sheet index 5. Separate `_tsPaths` array for non-standard directory locations
- **Half-tile collisions**: Hold Shift in collision editor → 24px snap instead of 48px
- **Player sprite**: `knights_1x.png` (312×288), 26×36 frames, RPG Maker 2K format. 3rd knight = cols 6-8 (dark visor, face hidden). 2.5x scale. Feet anchor. 4-dir facing from WASD
- **Walk animation**: Ping-pong cycle (0→1→2→1) — classic RPG Maker style
- **One-way walls reworked to non-blocking trigger zones**: Player walks through freely, hop fires only when holding WASD into zone for 0.15s
- **JumpStrength halved**: 280→140
- **Geometric shapes → triple combos**: Proposed mapping deferred ("another time")

### Technical Notes
- `_tsPaths` array for per-sheet file paths; sheet cycling uses `_tsSheets.Length`
- `_playerTex`, `KF_W=26, KF_H=36, KF_COL=6`, `_walkAnimTimer`, `_walkFrame`, `_facingDir`
- One-way CollRects: `continue` after hop check — never reach push-out code
