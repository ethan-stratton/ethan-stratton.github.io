# 2026-04-02

## Iron & Ether — Awakening Mode Progress

### Commits
- `3ebd7cc` — Massive bug fix batch (10 fixes): knight post-quest dialogue gate, door projectile FormType/EssenceType checks, room transition cleanup (pickup effects/obsidian/lava/portal state), portal failsafe, lava solid wall collision, lava puzzle redesigned to shield-dash stepping stones (no frost mechanic), Tome of Binding as standalone pickup, Grimoire SOTN effect + RefreshCombo, Room 2→3 door changed to Lance-locked.
- `87206a9` — Room 1 (The Gauntlet): 3-wave behavior combat room with damage resistance system. Swarm→Linger, Armored→Shatter, Evasive(teleporters)→Bloom. 25% damage from non-matching behaviors via FusionTag "weak_X" + BehaviorType on ProjectileParams. Torchlit pillars, floating embers. Rewards: Linger/Shatter/Bloom behavior crystals. Also committed Fantasy RPG Music Vol. 2 wav files.

### Design Decisions
- **Lava puzzle**: Removed frost/Silk mechanic (Silk isn't ice). Replaced with pre-placed stepping stones — shield-dash across gaps to reach island.
- **Tome of Binding**: Split from Nimue's Tear into standalone pickup in Room 7 (two items side by side on shrine).
- **Grimoire doors**: Require `_hasGrimoire && _loadout.FilledSlots == 3` (triple combo).
- **Wave doors**: Require any behavior equipped (`_loadout.Behavior != Behavior.None`).
- **Room 2→3 door**: Changed from Wave to Lance-locked.
- **Damage resistance system**: Enemies with FusionTag "weak_X" take 25% damage from non-matching behavior. BehaviorType added to ProjectileParams alongside FormType.
- **Portal failsafe**: UpdateRoom3 resets `_inPortal` if player is on screen != 3. Exit guard adds `!_portalTransitioning` to prevent double-trigger loop.
- **Room transition cleanup**: Now clears `_pickupFlashTimer`, `_pickupTextTimer`, `_pickupText`, `_pickupParticles`, `_obsidianPlatforms`, `_lavaRects`, `_inPortal`, `_portalTransitioning`, `_challengeActive`.

### Room Status (All 9 rooms)
- Done: 1 (Gauntlet/behaviors), 2 (Training Grounds/Lance), 3 (Proving Grounds/Caliburn), 4 (Knight's Hall/Shield), 5 (Awakening/start+wand), 6 (Wellspring/essences), 7 (Lady's Shrine/Nimue+Tome), 8 (Lava Sanctum/Grimoire)
- Remaining: 9 (The Bone Wyrm — boss room, needs design from Ethan)

### Intended Player Path
5→2→1(via 4)→6→4→7(shield-dash from 4)→3(Lance door from 2)→8(shield-dash stones)→9(boss)

### Open Bugs/TODOs
- Caliburn Shard "projectiles pierce enemies" effect not implemented
- Knight hit flash visual not applied during drawing
- T-menu relic section only handles 5 relics, needs 6th (Grimoire)
- Unowned relic tooltips show nothing on hover
- `_hasCaliburn` vs `_hasCaliburnShard` field mismatch
- Lightning bolt in Room 3 uses `_rng.Next()` in Draw (non-deterministic)

## Session 2 — Audio, Portal Fix, Boss (~01:30–03:05 UTC)

### Commits
- `f3b3f3a` — Portal fix (E key to enter, clamped position), procedural SFX (sine wave synthesis), lava stepping stone reduced to 1, 18 new dual combo names
- `84cd533` — Audio system: room-specific music from Fantasy RPG + Boss Battle packs. Songs loaded via Content Pipeline (OggImporter/SongProcessor). Music/ symlinks from pack ogg files. `PlayMusic`/`UpdateMusic`/`StopMusic` methods. Per-room track mapping in UpdateMusic.
- `91c0b7d` — Smooth music crossfade: fade-out→swap→fade-in. `MusicFadeSpeed=1.5`, `MusicMaxVolume=0.4`. `_musicFading`/`_musicPending`/`_musicVolume` fields.
- `f10c1c9` — Room 9 Bone Wyrm boss + quit→main menu

### Bone Wyrm (Room 9) Design
- 12-segment procedural bone snake, Moldorm AI (chase + wall bounce + smooth turning)
- Hades angular art style, bone-colored segments with rib detail
- Only tail (golden glow) is vulnerable — body deflects projectiles with sparks
- Tail hit → 2s rage mode (1.6× speed, 1.4× turn, red eyes)
- 4 phases at 75/50/25% HP: +20 base speed, +0.3 turn, lava pool spawns
- Phase 3+: periodic lava drip from body (every 3s, max 8 pools)
- 3s death sequence with bone fragment explosions
- Rewards: Haste Band + Phylactery
- Shatter behavior = 1.5× tail damage, triple combo = 1.3× multiplier
- Fields: `_bossActive`, `_bossDead`, `_bossHp`, `_bossMaxHp`, `_bossPhase`, `_bossSpeed`, `_bossBaseSpeed=140`, `_bossAngle`, `_bossTurnSpeed=2.5`, `_bossSegments[]`, `_bossSegmentAngles[]`, `_bossSegmentCount=12`, `_bossSegmentSpacing=16`, `_bossHeadSize=28`, `_bossTailSize=10`, `_bossLavaPools`, `_bossRageTimer`, `_bossHitFlash`, `_bossDeathTimer`, `_bossIntroTimer`, `_bossIntroDone`

### Other Changes
- Quit from pause menu → title screen (not exit). Only title "Quit" actually exits.
- Portal entry requires E key press (was auto-trigger on proximity — the glitch cause)
- 18 dual combo names added (Bolt/Lance × behaviors × essences for Awakening-available components)
- Procedural SFX via `GenerateSfx()` sine wave synthesis: pickup, essence, door, hit, dash
- Music packs: Fantasy RPG Music Vol. 2 (ambient/action/stingers), Boss Battle Music Pack Vol. 2

### ALL 9 AWAKENING ROOMS NOW COMPLETE
Full playable path from wand pickup to boss defeat.

## Session 3 — Major Bug Fix & Polish Pass (~04:00–05:37 UTC)

### Round 1 Fixes (subagent batch)
- Mode select UI: controls hint moved down, description text smaller with more spacing
- Gauntlet wave completion bug: enemies with SpawnTimer > 0 counted as dead → waves auto-cleared. Fixed by removing SpawnTimer check from allDead condition
- Renamed Impaler → Spiral Lance (shorter range 200→120, drill effect full length, follows player position, usable while moving)
- Renamed Silk Shot → Rainbow Thread
- Dash SFX softened: frequencies {300,200}→{180,100}, duration 0.05→0.03, volume 0.2→0.12
- Room 8 bridge: replaced with lever mechanism (press E)
- Portal fade transition overlay added
- Portal _challengeComplete init bug fixed (was stale from previous visit)
- Dual combo naming filter: ImplementedDualCombos HashSet — unimplemented combos don't show special names
- **BUILD ERROR**: subagent used `_currentKeys`/`_previousKeys` instead of `kb`/`_prevKb` — fixed manually

### Round 2 Fixes
- Dual combo flash/pulse when obtained (reuses _fusionFlashTimer)
- Room 8 bridge: 10 platforms edge-to-edge (later changed to solid bridge)
- Pedestals hidden when inventory open
- Tome of Binding added to inventory relic list
- Gauntlet: unlocks behaviors BEFORE each wave (Linger→wave 1, Shatter→wave 2, Bloom→wave 3)
- Merchant NPC in Room 5 after visiting 4+ rooms (hooded robed figure, E to interact, placeholder dialogue)
- Tome of Binding added to HUD relic icons (slot 8, letter "T")

### Round 3 Fixes
- Bloom + Lance homing: lance beams rotate toward nearest enemy at 3 rad/s when Bloom equipped (base interaction, no dual combo needed)
- Room 8 bridge: continuous solid stone bridge with lava damage exemption (not separate platforms)
- Portal interaction radius increased 28→45f, prompt 50→70f

### Round 4 Fixes (CRITICAL)
- **Grimoire pickup type "grimoire_awakening" had NO HANDLER** — animation played but _hasGrimoire never set, screen 8 never cleared. THIS was why grimoire didn't enter inventory and room 9 was inaccessible
- **Wand disappeared when leaving Room 5** — InitAwakeningRoomContent had no case 5, so returning to room 5 after transition cleared pickups without respawning. Added InitAwakeningRoom5Content()
- Knight now requires wand ("Come back with a weapon." if no wand)
- Room Cleared celebration: gold "Room Name Complete!" flash + particles + screen shake on every room completion
- Portal debug overlay added (later moved to Console.WriteLine)

### Round 5 (in progress — subagent running)
- Portal debug → Console.WriteLine (yellow text was covered by bolt)
- Gauntlet auto-equips matching behavior per wave
- Knight persists after room 4 cleared (extracted InitAwakeningRoom4Npcs)
- Merchant health bar (green, decorative, invulnerable)
- Bone Wyrm death improvements: segment drift, final explosion at 2.8s, projectile clear
- Victory music on boss death, peaceful music in room 9 thereafter
- Merchant endgame dialogue: "Try Bolt + Linger + Cinder, you won't regret it!" after all 9 rooms cleared

### Key Bug Patterns Discovered
- Pickup types must have explicit handlers in UpdatePickups (grimoire_awakening was missing)
- InitAwakeningRoomContent early-returns on cleared screens — must manually handle persistent NPCs
- _pickups.Clear() on screen transitions wipes everything; rooms need re-init cases
- Subagents sometimes use wrong variable names (_currentKeys vs _prevKb) — always verify compilation

### Portal Mystery
- Portal logic looks correct on paper but Ethan reports it still doesn't work
- All conditions checked: _challengeComplete properly initialized, _inPortal resets, portal position accessible, no wall blocking
- Added console debug logging to capture exact state on E press
- Interaction radius was small (28f) — increased to 45f
- Possible that Ethan never got a clean build since the _currentKeys compile error

## Session 4 — Portal Fix, Demo Polish, Room Borders (~05:37–17:53 UTC)

### Portal Bug SOLVED
- Root cause: Room slide transition completing on SAME FRAME as portal transition starting → blanket reset of `_portalTransitioning`
- Fix: `if (!_portalTransitioning)` guard in room transition completion code
- Additional: cancel `_inTransition`/`_transitionOffset`/`_screenTransitionTimer` when portal fade starts
- Additional: `_challengeWaveDelay <= 0` guard on challenge completion check (was auto-completing before enemies spawned)

### Demo Complete System
- New `GameState.DemoComplete` — scrolling credits after boss death + pickups collected
- Returns to boss room on Enter

### Death = Soft Restart (Awakening mode keeps all progress, respawn Room 5)

### Door System Improvements
- `UnlockDoorPair()` — both sides unlock when passing through either direction
- Room 3↔6 both Flame-locked, Room 6↔9 both ShieldDash, Room 9↔6 both ShieldDash
- Lance door: pulsing bullseye + "Shoot to open"; ShieldDash door: cracks/rubble + "Shield dash to break"

### Room Border System (NEW)
- `WallType` enum: Tree (Room 3), Stone (1,2,4,5), Water (6,7), Lava (8), Void (9)
- `DrawRoomBorders()` with themed walls, door archways, corners, screen-edge fill
- Water walls spawn ripple particles when projectiles exit
- Projectiles only bounce if `Params.Bounces` set; water/lava let projectiles through

### Gauntlet: doors lock during challenge, practice dummy before each wave, auto-equip behavior

### Knight: requires E, [E] Talk prompt, only spawns in Room 4, no dialogue loop

### Bone Wyrm: LavaPool struct with spawn/fade timers, segment drift on death, victory music

### Merchant: health bar only when damaged (500 HP), endgame dialogue

### Design Discussion
- Ethan wants modern environmental obstacles instead of key-doors for campaign
- Room borders: visible themed walls instead of invisible walls
- Room 2 redesign approved: pop-up targets, whack-a-mole style

### Open Issues
- Room 2 redesign (pop-up targets) — not yet implemented
- 15-second load time
- Dual/triple combo + weapon tuning pass needed
- Remove portal console debug before shipping

---

## Evening Session (~18:20–20:47 UTC)

### Marrow Bloom Triple Combo (Lance + Bloom + Bone)
- Implemented: homing bone lance beam that turns killed enemies into **Bone Husks** (static spiky obstacles)
- Bone Husks: 15 dmg/s to enemies, 5 dmg/s to player, block movement, destroyed by one player projectile
- 1.5-second gruesome death transformation: Seizure (vibrate, color drain) → Eruption (spikes burst out with blood particles + micro-shake) → Bloom (body swells/hardens) → Solidify (flash + crunch-shake)
- FusionName: `"MarrowBloom"` internal, `"Marrow Bloom"` display
- Bug fixed: position drift during seizure (shake was mutating husk.Position — moved to visual-only)
- Bug fixed: `deadEnemy.Color` → `deadEnemy.BaseColor` (Enemy class uses BaseColor)
- Bug fixed: initially fired as a square projectile instead of lance beam — rewrote to go through beam path like all lance weapons
- Bug fixed: Bloom+Lance homing excluded MarrowBloom (FusionName != null check) — added MarrowBloom to allowed list

### Room 8 Lava — Permanent Hazard
- User wants lava to ALWAYS be present in Room 8, even after clearing
- Extracted `InitRoom8Lava()` method, called from both `InitAwakeningRoom8()` and cleared-rooms early return

### Inventory Fixes
- Full-screen dark overlay (`DrawRect(0, 0, ScreenW, ScreenH, ...)`) instead of partial panel — wall borders no longer bleed through
- Relics column moved from `ScreenW/2 + 80` to `ScreenW - 280` to prevent overlap with form/behavior text

### Forbidden Laboratory
- Next.js app at `/home/node/.openclaw/workspace/forbidden-laboratory`
- Had build error: `Map` iteration needed `Array.from()` — fixed in `route.ts`
- Built and running on port 3000 via `next start`
- Start command: `cd /home/node/.openclaw/workspace/forbidden-laboratory && nohup node node_modules/.bin/next start -p 3000 -H 0.0.0.0 > /tmp/forbidden-lab.log 2>&1 &`
