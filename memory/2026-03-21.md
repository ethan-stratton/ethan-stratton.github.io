# 2026-03-21

## Late Night Session (cont. from 2026-03-20, ~22:37–00:23 UTC)

### EVE Scan System — Implemented (commit 12dbae9)
- Press E near enemy (200px) → 1s scan with green beam + particles + progress bar
- Scan Level 1: name label above enemy. Level 2: HP bar. Level 3: yellow pulse outline + 25% damage bonus
- HUD: top-right scan progress per enemy type ("EVE SCAN — Crawler 2/3")
- Reveal text flashes center-screen on completion
- `ApplyScanBonus()` helper applies +25% at level 3 for all melee damage (crawlers, thornbacks, hoppers)

### Ecosystem Trophic Interactions — Implemented
- Birds hunt Crawlers: every 2s, bird within 200px dive-kills nearest crawler (death particles)
- Crawlers flee birds: within 150px, reverse patrol direction
- Fields: `_birdHuntTimer`, `_swarmDamageTimer` (swarm not yet wired)
- Bird.cs: added `public int Hp = 1;`

### New Gyms Added
- **Scan Gym** (x=8500): 3 enemies behind walls for safe scanning, knowledge-gate door placeholder "ECOLOGY 2/3", open area with bird+crawler ecosystem demo
- **Ecosystem Gym** (x=9500): Left = predation zone (6 crawlers + 1 bird), Right = swarm zone (8 packed crawlers). Dividing wall between rooms.
- Debug room bounds expanded to right=11200, now 64 enemies total

### Ecosystem Design Doc Created
- `game-design/ecosystem.md` — full trophic level system (0-4), interaction matrix, player manipulation examples, 5 subversions
- Key subversions: Crawler swarm override (6+ = Level 3 threat), Burrower intelligence (learns player patterns), Floodwalker = planet's immune system, corruption inverts food chain, Babel Hive is actually a plant (the "minions" are pollinators)
- Ethan especially loved: ecosystem manipulation as gameplay (break wall → Grazer enters → Crawlers flee), and scan making enemies easier mechanically

### ApoxFox Video — "Your Indie Game's Story (probably) Sucks" (Y4KDzsbw9nc)
- 5 tips: start with gameplay not story, hook immediately, act like dungeon master not director, environmental storytelling > exposition, embrace subjective endings
- Validates our approach: knowledge gates = environmental storytelling that IS gameplay

### Outlined Text Rendering (commit 5c86d93)
- `DrawOutlinedString()` helper: 8-direction black outline on all text
- Applied to: spatial labels (editor+gameplay), death log, HP display, datapoint labels

### Design Decisions
- **Trophic level system**: assign level to new mob → automatic hunt/flee/ignore relationships
- **Subvert natural processes**: decomposer = predator, immune system = obstacle, hive = garden
- Knowledge gates feel like Zelda items, not homework — "you GET the scan data, world reveals things you walked past"
- Ethan confirmed: gameplay > story, knowledge should be accessible without heavy reading

### Git Commits
- `5c86d93` — outlined text rendering for gameplay labels, HUD, death log
- `12dbae9` — EVE scan system, trophic ecosystem interactions, scan+ecosystem gyms, scan damage bonus

### Scan System Redesign (~00:52–01:46 UTC)

**Research:** Reddit r/Metroid (Prime scanning complaints), YouTube CgA6XOuaT7Y ("Metroid Prime Had A Scan-tastic Story")

**Major design decisions (Ethan-confirmed):**
- **NO scanning live hostile enemies.** Scan corpses, sleeping/caged/trapped creatures. Creates trapping/camouflage/observation gameplay loops.
- **~10 scan entries per EVERYTHING.** Depth from different CONTEXTS not repeat presses.
- **Player NEVER learns native language.** EVE translates. Communicate via gestures/pictograms. Fetch quests = deduction.
- **Scan button = L** (no finger conflict with WASD)
- **Combat knowledge from combat** (kill counts → telegraphs → weak points). Scan = strategic.
- **EVE combat alerts** scale with scan depth: "Above you!" for Wingbeater dives
- **Knowledge Tree** branches: Ecology, Geology, Archaeology, Linguistics, Biology, Engineering

**Docs:** `game-design/scan-system.md` (full design), GDD updated, `game-design/ecosystem.md`

### Open Items
- Crawler swarm override not yet wired (timer exists, logic not implemented)
- Scan gym enemies behind walls — need to verify they can't escape but are scannable
- Knowledge-gated doors — visual placeholder only, no actual gate logic yet
- Parallax background layers loaded from `Content/backgrounds/bg_1/` (3 layers, different heights, bottom-anchored)
- Wall climb bugs fixed: floor collision during climb, squash/stretch reset on wall grab
- Bullet knockback + particles added for ranged hits

---

## Session Continued (~02:30–03:18 UTC)

### Tile Grid Origin Bug — ROOT CAUSE FIX (commit a1ad320)
- **The bug**: When tile grid was expanded to origin (-32,-400), ALL physics code still divided world coords by tileSize without subtracting origin. Every tile lookup was reading the WRONG cell.
- **Caused**: enemies falling through floors, slide stuck bug (HasHeadroom found phantom solid tiles), SnapToSurface placing enemies wrong
- **Fixed in**: `EnemyPhysics.cs` (rewrote with WorldToTileX/Y helpers), `Player.cs HasHeadroom`
- This was the real cause of the "slide into wall = stuck crouching" bug — HasHeadroom couldn't find clearance because it was checking wrong tiles

### Editor Cursor Snap Fix (commit f40f0bc)
- Changed `MathF.Round` → `MathF.Floor` for cursor, drag preview, ghost preview, entity placement
- Round snapped to grid LINES (tile boundaries), Floor snaps INSIDE grid cells

### Liquid Tile System (commit 5974822)
- Water (blue, slow), Lava (orange/red, fast), Acid (green, medium)
- Animated sine wave surface (two overlapping waves), depth darkening, rising bubbles, caustic light patterns, specular glints
- Added to both editor and gameplay draw passes
- Liquid pools added to debug room

### SotN-Style Debug Tiles (commit 6ba9b11)
- Purple/mauve fill with shade variation per tile (seeded by position)
- Warm tan/gold grid lines (not white)
- Inner highlight top-left, inner shadow bottom-right

### Slope Outlines Follow Diagonal (commit a1ad320)
- `DrawSlopeOutline` method handles all slope types: floor, ceiling, gentle, shaved
- Draws per-pixel diagonal edge + solid edges on appropriate sides

### Effect Tiles Distinct from Debug Tiles
- Added `IsEffectTile` branch in gameplay draw — Breakable, DamageTile etc. render with their own colored appearance instead of falling through to purple SotN style

### Dust Particle Fix
- Landing/dash dust now spreads horizontally (vy: -5 to -25) instead of flying up into ceilings (was -30 to -110)

### Wall Climb Chaining — Design Decision
- Ethan wants vault kick, dash, blade dash to chain INTO wall climbs (fun game design)
- Wall grab now properly cancels active move: clears IsVaultKicking, IsBladeDashing, IsCrouching, resets squash/stretch

### Git Commits This Sub-Session
- `f40f0bc` — editor cursor Floor snap
- `5974822` — liquid tiles system
- `6ba9b11` — SotN debug tiles, dust particle fix
- `b07f18c` — effect tiles, slope outlines, vault kick wall cancel
- `a1ad320` — tile grid origin offset in ALL physics (root cause fix)

### Continued Visual Polish (~03:18–03:57 UTC)

#### Debug Tile Style — Final Form
- Ethan wanted vibrant purple back (the stone-grey lost charm)
- **Final**: vibrant purple fill (R:140,G:80,B:160 + shade variation), gold 3D-bevel (light gold 190,160,100 top-left, dark gold 80,60,30 bottom-right)
- Commit `4cfd486` — reverted to vibrant purple + gold bevel

#### Slope Outline Saga (~5 iterations)
- Full box around slopes → stray lines in empty triangle space
- Diagonal only → no grid definition between adjacent slopes
- Full grid + diagonal → outline around empty space (user: "that's what we DON'T want")
- Solid-edge-only → stray verticals on tall side
- **Final (commit 6e8f4b7)**: outlines traced from actual DrawSlopeTile fill geometry row-by-row
  - Each slope type's diagonal edge computed from the same math as the fill
  - Straight edges only where fill touches tile boundary (e.g. gentle floor has vertical on solid side from halfway down)
  - Shaved tiles: diagonal on shaved corner, straight edges on remaining 3 solid sides (partial where shave cuts in)
- **Key lesson**: never approximate slope outlines — always derive from the exact fill computation

#### Other Fixes in This Block
- Zoo enemy spawn Y: moved from 788 (inside tile stack) to 668 (above surface at y=688)
- Afterimage struct now stores `DrawHeight` — no ghost rectangle during crouch/slide
- Dust particles spread horizontal (vy was -30 to -110, now -5 to -25) — no more particles into ceiling
- Scan gym walls extended (was 48-96px, now 248-336px tall from y=400)

#### Commits
- `4cfd486` — vibrant purple + gold bevel
- `0f4ca11` — diagonal-only slope outlines
- `774efe5` — full grid border + diagonal on slopes
- `7d45652` — solid-fill-only edges
- `f176542` — remove stray verticals, add shaved diagonals
- `6e8f4b7` — final: outlines from actual fill geometry (HEAD)

---

## Session Continued (~04:00–05:17 UTC)

### Gentle4 Slope Outlines (commit a2252b7)
- All 16 Gentle4 (4-tile smooth) slope types had zero outline handling — got purple fill but no gold edges
- Added diagonal edges (per-pixel from fill geometry) and straight edges for all 16: UpRight A-D, UpLeft A-D, CeilRight A-D, CeilLeft A-D

### Bug Fixes
- **Uppercut breaks breakable tiles** (commit 87ebd05) — breakable check only ran for MeleeTimer, added IsUppercutting check with UppercutHitbox
- **Dust particles splatting on wrong surface** (commit 87ebd05) — floor collision checked `p.Y >= sf.Top` with no upper bound, causing particles below blocks to splat on top. Fixed: requires within 8px of sf.Top AND downward velocity
- **Particles fall through slopes** (commit eafaa25) — added `GetSlopeFloorY` check for falling particles, splatter on slope surface
- **Build fix** (commit after) — used wrong variable names `_screenShakeTimer`/`_screenShakeMagnitude` instead of `_shakeTimer`/`_shakeIntensity`

### Debug-Room-2 + Switch System + Crawler Swarm Override (commit 0d4d6b7)
- New level `debug-room-2.json` — enclosed tile room accessible via portal from debug-room (x=10600, labeled "MORE DEBUG WORLD")
- **Switch system**: JSON-driven interactable objects, press W to activate, gold appearance with [W] prompt, screen shake on activate. `SwitchData` class in LevelData with id, position, action, label
- **Crawler.Frozen**: blue-tinted, no movement/aggro. Spawnable from JSON with `"frozen": true`
- **Crawler Swarm Override**: 4+ non-frozen crawlers within 100px = swarm cluster → converge on nearest dummy target → collective damage (count × 2) when 4+ touching target
- Demo: 6 frozen crawlers + 1 large dummy + RELEASE switch

### Crawler AI Overhaul (commit 3caeade)
- **Jumping**: short hops when chasing player (force -200, 1.5s cooldown), aggressive forward leaps in swarm mode (force -300, 0.6s cooldown)
- **Full surface patrol**: crawlers now walk entire platform edge-to-edge instead of ±100px from spawn
- **Swarm convergence**: SwarmTargetX drives crawler AI, surface edge clamping disabled during swarm, swarm speed 130 (vs 60 patrol / 100 chase)
- **Landing squash/stretch** on ground impact
- `SwarmActive` (red tint), `SwarmTargetX`, `SwarmSpeed`, jump constants added to Crawler.cs

### Crawler Latch-On Mechanic (commit 42c852d)
- Aggroed crawlers latch onto player on contact (replaces contact damage for latch-capable crawlers)
- Latched crawlers stick to player body at random offsets, deal 1 damage every 0.3s
- Max 4 latched at once, 0.4s stagger between latches
- Removal: melee hits detach + knockback, dash/slide/uppercut/vault kick/blade dash/cartwheel/flip fling 60% off
- 1.5s cooldown before re-latch. Dark red tint when latched
- **Future variants**: some only detachable via water/lava submersion or high altitude

### EVE Speech Bubble System (commit 9bedeab)
- `EveAlert(msg, duration)` — timed speech bubble near orbiting EVE orb with black background box and fade-out
- Latch warnings now EVE dialogue with 4 random variants ("Hostile organism attached!", "Parasite detected on suit!", etc.)
- Removed static HUD "LATCHED x#" text
- All movement actions (slide, uppercut, vault kick, blade dash, cartwheel, flip) detach crawlers

### Metroid Prime Design Analysis
- Video: "5 Brilliant Ideas that make Metroid Prime the BEST GAME EVER" by Snulch (6H3vu4pCHbo)
- Saved full analysis to `memory/2026-03-21-metroid-prime-analysis.md`
- 5 key ideas: Scan Visor (validates EVE), Environmental Storytelling, Obsessive Detail (#3 paramount per Ethan), Evolving Music, Solitude Not Isolation
- Ethan: "remember those 5 points" and "#3 will be paramount"

### TranscriptAPI Key
- Key: stored in ~/.bashrc as TRANSCRIPT_API_KEY
- Working: successfully fetched Metroid Prime video transcript

### Voronoi Caustic Water (commit cc7c4dc)
- Replaced sine-wave caustics with Voronoi cellular noise (d2-d1 ridge detection)
- Animated seed points in 3x3 cell neighborhood, golden-green ridges on dark base
- Computed at 2px resolution for performance. Bubbles kept for lava/acid only
- Reference: Ethan sent two images of animated noise water with bright ridges between dark cells

### Weather System (commit cc7c4dc)
- **Rain**: directional droplets, splash particles on ground impact, ~5 spawns/frame
- **Storm**: heavy rain (12/frame) + dark puffy clouds + random lightning (white flash + screen shake, 2-7s interval)
- **Wind**: pushes rain sideways, moves clouds faster
- **Clouds**: layered rects (3 layers for puffiness), variable opacity, darker during storms
- **EVE announces** weather changes contextually
- **Toggle switches**: weather switches re-pressable (vs one-shot switches like swarm release)
- Screen-space overlays: lightning flash, ambient darkening during storm/rain

### Debug-Room-2 Expanded (commit cc7c4dc)
- Extended from 25 to 125 tiles wide (5 sections with dividing walls + openings)
- Room 1: Swarm demo | Room 2: RAIN | Room 3: STORM | Room 4: WIND | Room 5: EVERYTHING

### Design Doc Addition
- Principle #20: Enemy Personality Through Environmental Interaction
- Giant lizard aggressive only with EVE glowing, poison moth attracted to armor type, wolf chases thrown sticks
- Every major enemy should have at least one non-combat interaction

### Ethan's Ideas for Tomorrow
- Option B: EVE Scan Result Display (proper holographic popup with creature diagrams, anatomy callouts, bestiary logging)
- Option C: Room Transitions + Momentum Carry
- Both deferred to next session

### Water Rendering — UNRESOLVED
- Voronoi caustics look like alien slime/stained glass, NOT realistic water
- Three iterations: bad hash (grid pattern) → thick ridges (reptile scales) → Power(5) thin ridges (cracked glass)
- **Ethan's verdict**: save the Voronoi look for corrupted/alien liquid (purple variant). It's cool, just not water.
- **TODO tomorrow**: research sine wave interference approach (how real caustics actually form — additive light patterns from wave refraction, not cell boundaries)
- Reference images Ethan sent: dark cells with bright golden ridges, animated with noise — the look is more "light dancing on pool bottom" than "cell walls"
- Video watched: Sasquatch B Studios Unity water tutorial (TbGEKpdsmCI) — mesh + shader graph approach, Voronoi + Power node
- Research: Cyanilux water shader breakdown confirms Voronoi → Power(5) → strength, but this still looks wrong for pixel art 2D

### Git Commits This Sub-Session
- `a2252b7` — Gentle4 slope outlines
- `87ebd05` — uppercut breaks breakables, dust particle floor fix
- `eafaa25` — particles collide with slopes
- `0d4d6b7` — debug-room-2, switch system, crawler swarm override
- `3caeade` — crawler AI overhaul (jumping, full patrol, swarm convergence)
- `42c852d` — crawler latch-on mechanic
- `9bedeab` — EVE speech bubbles, all movement actions detach crawlers
- `cc7c4dc` — Voronoi water, weather system, weather demo rooms, design doc #20
