# 2026-03-31

## Iron & Ether — Session 8 (~23:58-00:17 UTC)

### Camera & HUD Fixes
- **HUD no longer moves with camera**: Split Draw into two SpriteBatch passes — game world uses camera transform (scroll + shake), HUD uses untransformed batch
- **Micro-follow camera REMOVED**: Ethan found it "super annoying" — not right for the game. Code disabled, constants kept but unused
- **Screen transitions fixed**: Directions were inverted (scrolled wrong way). Flipped all 4 `_transitionDir` vectors
- **Room number**: Now gold lettering (`220, 180, 50`), inside arena (bottom-right), just the digit (no "R" prefix)

### Hiten Mitsurugi (Lance+Shatter+Silk) — v4 Final
- Ethan's vision: NOT a beam. A battojutsu slash — one devastating hit, miss and you're screwed. Major spectacle.
- **5× damage multiplier** — one-hit-one-kill territory
- **2.5s fire rate recovery** — massive punishment window on miss
- **0.15s hitstop** on enemy hit — game freezes, sells the cut weight
- **Hitstop system added**: `_hitstopTimer` field, checked at top of Update(), skips all game logic when active
- One massive 130px silver-white blade (not beam segments), with lavender outer glow + white razor edge
- 4 afterimage copies trailing behind blade (offset backward along swing dir, fading lavender)
- 24 white speed line particles + 10 perpendicular lavender silk wisps on fire
- 16 impact particles per enemy hit (white + lavender burst) + extra trauma
- `FusionName = "HitenSlash"` / `"HitenAfterimage"` for draw routing in Projectile.cs
- Draw: 3-layer (glow → body → edge) for slash, single faded layer for afterimage
- Ethan feedback progression: v1 (12 segment arc + afterimages) → "doesn't look like katana" → v2 (single big blade) → "doesn't look like slash, no afterimage visible" → v3 (thin lance beam) → "still looks like beam, hitstop weak, fire rate too high" → v4 (current spectacle version)

### Key Design Insight
- Ethan wants Hiten Mitsurugi to be high-risk/high-reward: "one-hit-one-kill but if you miss you're screwed" — this is a fighting game mentality, not RPG. Commit-or-die moves.

### Implemented Fusions: 20/23
- Added to list: Hiten Mitsurugi (Lance+Shatter+Silk)
- Total: Pillars of Muspelheim, Dragonbreath, Fimbulwinter, Vesuvius, Gungnir, Indra's Arrow, Surtr's Crown, Death's Scythe, Event Horizon, Gebel's Swarm, Morrigan's Court, Yggdrasil's Root, Chronos' Chain, Samael's Embrace, Katyusha, Shango's Shale, Surya's Wrath, Longinus, Dragon's Teeth, Hiten Mitsurugi

### Remaining Fusions: 3
- Rift Tide (Wave+Linger+Hollow)
- Gae Bolg (Bolt+Stasis+Pulse)
- Grave Call (Seed+Shatter+Bone)
- Tiamat's Garden (Wave+Bloom+Sap)

### Pending User Feedback
- Screen transition direction fix — needs testing
- Hiten Mitsurugi v4 — needs playtesting
- All prior fusion reworks still pending verification

## Iron & Ether — Session 9 (~00:17-00:45 UTC)

### Lance Beam Overhaul
- **Fundamental change**: Lance no longer spawns 20+ individual segment projectiles. Now spawns ONE projectile with `BeamLen` field representing full length
- `BeamLen` added to `Projectile` class. `HitBox` getter returns elongated AABB if `BeamLen > 0`
- Draw code: single continuous rotated rectangle with 3 layers (glow → body → core), all centered on same Y origin
- All fusion-specific beam rendering preserved: Gungnir (purple), Surya (fire), Longinus (icy), HitenSlash (silver-white), HitenAfterimage (lavender fade)
- Hiten Mitsurugi firing code updated to use new beam system (BeamLen instead of Size for length)
- **User feedback on base lance**: layers were visually disjointed (3 separate lines). Fixed: each layer now uses `height / 2f` as own center, glow opacity reduced

### Gorgon's Grasp (Volley+Stasis+Sap) — NEW FUSION
- Added to SpellFusions dictionary and IMPLEMENTED_FUSIONS block
- Concept: snakes behind player face cursor, fire poison at enemies
- **v1 issues** (user feedback): no visible snakes (just particles), bullets auto-targeted enemies instead of firing at cursor
- **v2 rework**: 
  - `GorgonSnake` struct with `AngleOffset` and `Phase` fields, stored in `_gorgonSnakes` list
  - 5 procedurally animated snakes drawn every frame (10 segments each, sinusoidal slithering)
  - Body curves from behind player toward cursor via quadratic lerp
  - Alternating dark/light green scale pattern, yellow eyes on head
  - Snakes fire ONLY in cursor direction (not auto-targeting), slight per-snake spread
  - Bite (<40f to enemy head): 2× damage, 1.5s stasis. Spit (else): poison glob, 200 speed, 0.8s stasis
  - `_gorgonTime` field drives animation, updated in main Update loop
- Snakes spawned when fusion activated, cleared when changed away

### Git Commits
- `ca76d47` — Lance overhaul: single beam projectile, Gorgon's Grasp initial
- `d88b253` — Fix lance beam layers (centered), Gorgon procedural snakes with slithering

### Key Identifiers
- `Projectile.BeamLen` — beam length from origin to tip
- `GorgonSnake` struct, `_gorgonSnakes` list, `_gorgonTime` float
- FusionName tags: `"GorgonBite"`, `"GorgonSpit"`
- Fusion key: `(Form.Volley, Behavior.Stasis, Essence.Sap)` → "Gorgon's Grasp"
- 21 implemented fusions now (added Gorgon's Grasp)

## Iron & Ether — Session 10 (~01:00-01:35 UTC)

### Gorgon v3: Autonomous Snakes
- Reworked Gorgon's Grasp from click-to-fire to fully autonomous
- Each snake has own `AttackTimer`, `TargetId`, `StrikeTimer` fields
- Scans nearest enemy within 250f, spits at >55f (0.9s cd), bites at <55f (0.6s cd)
- Bite lunge: last 3 segments extend 20px forward in 0.15s
- `BuildProjectileParams()` helper added so snakes can create projectiles outside FireWeapon
- Old Gorgon code removed from FireWeapon, replaced with `return` guard
- Commit: `5bdd931`

### Hit Juice & Death Effects
- **Knockback**: `KnockbackVel` field on Enemy, applied on projectile hit, force = 120 + dmg×3, fast exponential decay
- **Squash**: Enemy shrinks to 70% on hit, springs back over 0.1s via `SquashTimer`
- **Universal death explosions**: 10+ particles in enemy color + 4 white flash particles, trauma scaled to enemy size
- **Kill streak counter**: `_streakCount`, `_streakTimer` (2s window). Labels: 3+=STREAK, 5+=KILLING SPREE, 10+=UNSTOPPABLE, 15+=RAMPAGE, 25+=MASSACRE. Pulsing colored text at arena top
- Commit: `d63e21f`

### Aether Shard Drops
- 3 tiers: cyan (val 1, 85%, 4px), blue (val 5, 12%, 5px), gold (val 25, 3%, 6px)
- Scatter on spawn (0.3-0.5s), then magnetic pull within 80f, collect at 12f
- Sparkle particles on collect, blink before 8s despawn
- `_aetherShards` counter on HUD — no functionality yet, just increments
- Commit: `7f41fe8`

### 6 Enemy Types (Major Feature)
- **EnemyType enum** added: Squire, GuardGargoyle, HollowPlate, ShieldMaiden, HedgeSniper, TowerGuard
- **Enemy projectile system**: `EnemyProjectile` struct, `_enemyProjectiles` list, damage player on contact
- **PendingShots** pattern: Enemy stores shots, Game1 consumes and spawns each frame
- **Shield blocking**: ShieldMaiden + TowerGuard block 85% frontal damage. `TakeDamage` now accepts `sourceDir`
- **Per-type AI**:
  - Squire: walk + 4-way shot every 2s
  - GuardGargoyle: hop to random spot near player (2.5× speed), 8-shot AOE burst on land, rest, repeat
  - HollowPlate: pursue until <120f, backpedal + seeking shots
  - ShieldMaiden: slow turn (2.5 rad/s), walks toward player, pure tank — strafe to counter
  - HedgeSniper: stationary, hidden (15% opacity) → pop-up → fast bolt (300 speed, 15 dmg) → hide. Glass cannon (0.5× HP)
  - TowerGuard: slow (0.4× speed), 2× HP tank, 3-shot burst spread, shield
- **Wave progression**: waves 1-2 squires only, 3-4 add gargoyle+hollow, 5+ full roster
- **Visual details**: gargoyle wings+horns, hollow plate visor slit, sniper red targeting dot, shield bar rendering
- **Drop colors removed from type** — enemies now use type-specific base colors instead of drop-color override
- Commit: `ae0c3a9`

### Key Identifiers (new)
- `EnemyType` enum in Enemy.cs
- `Enemy.PendingShots`, `Enemy.ShootTimer`, `Enemy.FacingAngle`, `Enemy.AiStateTimer`, `Enemy.AiPhase`, `Enemy.ShieldUp`, `Enemy.TurnSpeed`, `Enemy.HopTarget`
- `EnemyProjectile` struct + `_enemyProjectiles` list in Game1.cs
- `_streakCount`, `_streakTimer`, `_bestStreak` fields
- `_aetherShards` int, `AetherShard` struct, `_shards` list
- `_enemyProjectiles` drawn with glow+core+bright-center pattern

## Iron & Ether — Session 11 (~01:35-03:07 UTC)

### Wave Boss System
- Bosses spawn after hitting wave kill target (5 + wave×2), arena clears first
- Boss stats: 2× HP, 28px size, pulsing color aura, dedicated HP bar
- Type rotates: Gargoyle → HollowPlate → TowerGuard
- Drop color coding: wave 1 red (form), wave 2 blue (behavior), wave 3 green (essence), then cycle
- Normal enemies no longer drop components in wave mode — boss only
- Wave banner: "BOSS" on spawn, "WAVE X" on completion
- Commits: `1273f16`, `e3f3e1a`

### Senbonzakura Fusion (Wave+Bloom+Silk)
- 7-10 piercing petal blades per burst (0.12s fire rate), ~70° cone, 0.18× base damage each
- Bloom growth (0.4-0.7 rate, 1.5× max), sine wave drift (18-30px amp), 2.5s lifetime (reaches screen edge)
- Custom petal draw: elongated diamond with tumble rotation, white edge highlight, glow
- Ambient petal aura orbits Morien (2 particles/frame, tangential velocity)
- Blood jet spray on hit: 8 pure red particles per petal impact in projectile direction
- Design philosophy: low per-hit damage, insane volume — death by a thousand cuts
- **Bug found/fixed**: Initially placed firing code inside `Form.Volley` branch — unreachable for Wave form. Moved before form branching
- Commits: `dd6686a` (initial), `bd704a2` (fix), `70d4016`→`b6bef6e`→`d5cf59e`→`c08c113` (tuning)
- FusionName tag: `"Senbonzakura"`

### Essence-Specific Hit Effects (7 types)
- **Cinder**: orange/yellow flames rising + floating embers
- **Dark**: purple/black shadow wisps curling outward
- **Bone**: white/tan chip fragments scattering with gravity
- **Silk**: white/lavender thread wisps, slow/floaty
- **Pulse**: bright yellow/white electric sparks, fast+short
- **Hollow**: dark purple particles imploding INWARD (reverse burst)
- **Sap**: green particles flowing from enemy toward player
- Uses `_loadout.Essence` (not ProjectileParams.Essence which doesn't exist)
- Fusion-specific hit effects (e.g. Senbonzakura blood) layer on top

### Boss Kill Spectacle
- 0.3s hitstop freeze frame, max trauma
- 40-particle explosion ring in boss color, 8 white core flash
- 15 bonus shard rain (mixed tiers)

### Player Damage System
- `DamagePlayer(amount, sourcePos)` helper method — centralizes all damage
- 0.8s invincibility frames (`_iFrameTimer`), rapid sprite flicker at 20Hz with red tint
- 10 blood splatter particles spray away from hit source
- Red screen flash overlay (0.25s, 35% alpha) via `_damageFlashTimer`
- Screen shake on every hit (0.25 trauma)
- Contact damage changed from per-frame tick to single hit + iframes

### Commit: `4f496f8`

### Ethan Feedback
- "bigger and more bombastic is better for our visual effects" — core design principle
- Senbonzakura petals: initially too big (covered half screen), tuned down, then swirl amped back up
- Blood spray: initially too subtle (3 small particles), amped to 8 large pure-red particles
- Wants custom per-fusion hit effects layered on (e.g. Gorgon's Grasp should look different)
- Next priorities: remaining fusions, enemy spawn effects, room variety

### Key Identifiers (new)
- `_iFrameTimer`, `_damageFlashTimer` fields
- `DamagePlayer(float amount, Vector2 sourcePos)` method
- `_bossAlive`, `_bossId`, `_waveBannerTimer`, `_waveBannerText` fields
- `SpawnBoss()` method
- Boss visual: pulsing aura drawn before enemy.Draw(), size = enemy.Size + 6 + sin pulse
- Senbonzakura in SpellFusions dict: `(Form.Wave, Behavior.Bloom, Essence.Silk)`

## Iron & Ether — Session 12 (~03:07-04:23 UTC)

### Essence Hit Effect Scaling by Combo Tier
- Single slot: no essence hit particles. Dual combo: base particles. Spell fusion: 2.5× particles, 1.6× size, 1.4× lifetime, 1.3× speed
- Commit: `bb159e4`

### Enemy Spawn Animations (6 types)
- GuardGargoyle/TowerGuard: shadow→slam. HollowPlate: assembles from ground. HedgeSniper: grows like plant. Squire/ShieldMaiden: portal fade-in
- Enemies invulnerable + no AI during spawn, no player collision
- Ethan: "couldn't verify most existed" — not visually remarkable but system works
- Commit: `5a960c8`

### Game State Machine
- `GameState` enum: TitleScreen, ModeSelect, Playing, Paused, GameOver
- `GameMode` enum: Campaign, Gauntlet
- ESC pauses gameplay, game over shows stats with 1s input delay
- `ResetGame()` clears all state. Gauntlet auto-grants wand+shield
- Commit: `a01e95a`

### Title Screen & Content Assets
- Ethan provided: `morien_portrait.png`, `wand_gun.jpg`, `SymphonyoftheNightfont.ttf`
- Portrait fades left, wand bobs right, dark gradient for text
- Removed subtitle per request. Fixed centering with MeasureText/DrawTextCentered helpers
- SotN font not yet wired through content pipeline
- Commit: `302b60c`

### TAB Inventory Panel (Plague Knight-style)
- TAB toggles sliding overlay from HUD over arena, pauses game when open
- Left: wand config with unlocked options. Right: relics + stats
- Current combo name shown (gold=fusion, sky blue=dual)

### Ethan's Direction
- Moving toward "real game" — menus, modes, proper HUD
- Wants Zelda 1-style extended top HUD with Plague Knight bomb-crafting-style wand config
- Quick switching (1/2/3) stays, TAB opens detailed inventory
- Eventually: multiplayer, more game modes (Geometry Wars-style)

## Iron & Ether — Session 13 (~04:23-05:10 UTC)

### HUD Overhaul (3 commits: 3dc8743, 6254ec4, b5785b6)
- **Layout redesign**: Row 1 left = 3 wand slot boxes (FORM/BEHAVIOR/ESSENCE at x=10,200,390). Row 1 right = pixel hearts + shards + relic icons. Row 2 = combo name + stats + wave/kills
- **HP bar → pixel hearts**: 10 hearts (each = 10 HP), full=red, empty=dark outline, partial fills from left
- **Boss HP bar moved**: From above arena (covered HUD) to inside arena bottom
- **Inventory panel (TAB)**: Covers full screen from y=0, hides normal HUD when open, extracted to `DrawInventoryPanel()` method
- **SotN font glyph issue discovered**: Font only has letters — no digits, colons, symbols. Fix: SotN for names/headers only, DefaultFont for numbers
- **SotN font upscaled**: 16 → 24pt
- **Inventory overlap fixed**: Right column pushed to `ScreenW/2 + 140`
- **Relic tooltip array fixed**: Was 5 items for 4 relics

### SotN Font Cross-Platform Fix (commit b74df78)
- MGCB couldn't find font on macOS. Fix: `<FontName>` references .ttf filename directly
- Font registered as `SymphonyoftheNig` (truncated) on both platforms

### Key Lesson
- **SotN font is letters-only** — always use DefaultFont for text with digits/colons/symbols

## Session 14 (~05:23-06:10 UTC) — Fonts + Shadow Stitch

### Font Experiments
- **Karmatic Arcade (ka1.ttf)**: Ethan's favorite look, BUT has visual line artifacts at most sizes. Tried 16pt and 40pt — lines persisted at both. Needs specific sizes (maybe 72pt?) to render clean. Problem unsolved.
- **Return of Ganon (retganon.ttf)**: Ethan liked it, wanted bigger → bumped to 24pt
- **Timetwist**: Good body font with full punctuation support. Attempted dual-font system (RetGanon headers + Timetwist body) but it became "an unreadable mess" with ka1 at 40pt
- **Final state**: Reverted ALL font experiments back to original SotN font (`git reset --hard 865a616`). Clean slate. Commit `1ffc526`.
- **Lesson**: Don't chain font experiments — each one compounds mess. Reset to known good state between attempts.
- **Ethan wants to revisit fonts tomorrow** — ka1 is the dream font if artifacts can be solved

### Shadow Stitch — New Fusion (Bolt+Possess+Dark)
- Inspired by **Shikamaru Nara** from Naruto (Shadow Possession Jutsu)
- Name: "Shadow Stitch" (Ethan chose it)
- **v1** (commit `87b51c8`): Standard bolt with tether particles. Ethan couldn't see the tether — too small/faint
- **v2** (commit `bb4979f`): Complete rework per Ethan's vision:
  - Shadow HAND extending from player, not a bullet
  - Tapered arm drawn from player to hand (thin→wide) with 5 spread fingers
  - 1.5s fire rate (slow, deliberate)
  - Hand grows 12px→20px as it travels
  - At max range (0.8s), lingers and slowly chases cursor (up to 4s)
  - On hit: 0.5s freeze → possess → shadow tendrils fork to 3 nearby enemies (root+slow)
- **Ethan says it needs more rework** — will iterate tomorrow
- Key fields: `_shadowTethers` list, `_mouseWorld` field, `FusionName = "ShadowStitch"`
- Added to SpellFusions dict and IMPLEMENTED_FUSIONS list

### Technical Notes
- `_mouseWorld` field added to Game1 for projectile cursor tracking
- Projectile lifetime hack: `Params.Lifetime = Age + 0.1f` to prevent death while lingering
- Draw uses segmented arm (tapered rectangles) + finger fan at tip

## Iron & Ether — Session 15 (~13:16-14:21 UTC)

### Shadow Stitch v3: Channeled Chain-Possess (commit `2b0a7ef`)
- Complete rework from v2 → channeled chain mechanic
- Fire ONE shadow hand (LMB), on hit: possess+freeze enemy, anchor Morien
- Auto-tendrils chain to nearest unlinked enemy (150f range), repeats
- Release LMB → damage all chained (8 + 12 per enemy), dark explosions, scaled hitstop
- Hand has thumb now, wobble animation
- Old `_shadowTethers` → `ShadowStitchState` struct; `GetEnemyPos()` helper added
- **Ethan tested: "works great!"**

### Sacred Fist Fusion (commit `fe63318`)
- **Nova + Shatter + Bone** → massive bone fist punch, short range
- `FireRate=0.9f`, `BaseDamage*=4f`, `Speed=350f`, `Lifetime=0.25f`, `Size=22f`, `Piercing=true`, `ShatterCount=5`
- Custom draw: bone fist body, knuckle ridge, joint lines, wrist, outer glow
- Hit: 0.12s hitstop, 16 bone shrapnel, 12 shockwave ring particles, 400f knockback
- `FusionName = "SacredFist"`

### Clickable TAB Inventory (commit `fe63318`)
- Click unlocked component names to equip; hover → white; active → gold
- `RefreshCombo()` extracted as reusable method
- Click active item to deselect (toggle to None) → dual combos (commit `01283f4`)

### Inventory Spacing Overhaul (commit `604cfd8`)
- Panel 320→420px; removed "Available:" labels; hidden None from lists; empty = "---"
- More vertical spacing (30px sections, 24px label-to-options, 16px between items)
- HUD: HP y=8, shards y+18, relics y+38 (fixed overlaps)
- "TAB to close" inside panel at bottom

### Pending
- Layout feedback not yet received from Ethan
- Sacred Fist needs playtesting
- Vajra → lightning mentioned, not started

## Session ~14:30-15:03 UTC — Ether Rename, Phylactery, Audit & Bug Fixes

### Ether System Rename
- Mana → Ether everywhere (EP label, fields, comments)
- Aether Shards → Ether Shards — ties currency to game title
- "Iron & Ether" = iron frame + Fae energy; ether IS the mana
- Gun overheat glow added: below 40% ether, wand glows orange-red with layered sine flicker. Visual only.

### Merlin's Phylactery Relic
- +25% max ether (100 → 125 EP)
- `_hasPhylactery` bool, `EffectiveMaxEther` property
- Purple "P" HUD icon, inventory entry
- Commit `3796f72`

### Code Audit — Bugs Found & Fixed (commit `c5596d6`)
1. **Ether reset order** — EffectiveMaxEther was read before _hasPhylactery reset → ether started at 125 with cap 100. Fixed: moved ether reset after relic flags.
2. **Particle cap** — 86 spawn sites, no limit. Added 2000 cap with oldest-first removal.
3. **Shard cap** — same issue, deferred (less critical).
4. **Shadow Stitch in enemy loop** — was gated by `if (i == 0)` inside enemy loop; empty list = frozen tendrils. Moved to standalone block before enemy loop.
5. **Stale _mouseWorld in inventory** — now updated before inventory early-return.
6. **Inventory layout drift** — Update and Draw computed positions independently. Extracted shared constants: `InvPanelX`, `InvHeaderH`, `InvLabelH`, `InvRowGap`, `InvItemIndent`, `InvItemGap` + computed properties for Y positions.

### Architecture Discussion
- Recommended partial classes for splitting Game1.cs (6200+ lines): Update, Draw, Fusions, Enemies, UI, core
- Big O: O(n²) enemy scanning everywhere. Proposed fixes:
  1. Enemy ID dictionary (trivial, eliminates ~15 linear scans)
  2. Spatial grid (80×80 cells, the real solution for 50+ enemies)
  3. Projectile pool / compact
  4. Batch collision
- Ethan asked about all of these; offered to wire up enemy ID dict next

### Decisions
- Morgause shelved for now ("lets forget about morgause")
- Ether bar label: "EP" (not "Ether" spelled out)
- Gun overheat is visual only, no gameplay penalty
- "Ether Shards" not "Aether Shards"

### Commits
- `3796f72` — Ether rename, Phylactery relic, gun overheat glow
- `c5596d6` — Bug fixes (6 items)

## Iron & Ether — Sessions 18-19 (~19:00-20:28 UTC)

### Creeping Moss (Seed+Linger) — commit `5f01c01`
- Growing linger zone: 20px→80px over 12s, slows enemies, low damage
- Organic mossy patch visuals, spore particles
- New LingerZone fields: GrowRate, MaxRadius, SlowFactor; FusionTag "CreepingMoss"

### COMBO-DESIGNS.md — commit `82bc3e8`
- Design notes for all 58 dual combos + Brahmastra spell fusion
- Forbidden Laboratory API serves design notes (commit `9d53065`)

### Seed Overhaul — commit `76c5487`
- Arc trajectory (parabola player→cursor, shadow, glow trail, age resets on landing)
- Countdown pulse (accelerating 3Hz→18Hz, warning crosshair last 2s)
- Detonation juice (burst + dust ring + shake)
- Chain reaction (60px range)
- New Projectile fields: ArcStart, ArcTarget, ArcDuration, ArcTimer, ArcLanded, ArcHeight

### Wall System — commit `2e9e692`
- Per-screen walls: `_screenWalls` Dict<int, List<Rectangle>>
- 5 rooms: Screen 5 (pillars), 4 (corridor), 6 (L-shaped), 2 (pillared hall), 8 (three lanes)
- Player/enemy wall collision (min-overlap push-out)
- Projectile-wall: die or bounce (with sparks)
- Beam truncation via ray-AABB intersection
- Chain LOS blocking via `HasLineOfSight()` helper
- Nova stops at walls naturally

### Campaign Screen-Bound Enemies
- `_screenEnemies` dict, `_clearedScreens` hashset — cleared stays cleared
- Enemies saved/loaded on screen transition

### Ethan Design Decisions
- Cleared-stays-cleared rooms approved
- Brahmastra (Volley+Bloom+Cinder) approved as nuke fusion
- Bolt+Cinder = huge parabolic arc (Ethan's idea)
- Seed cooking mechanic discussed, not built yet

### Next Steps
- Enemy pathfinding around walls
- Locked doors / room clearing gates
- Room-specific enemy spawns for campaign

## Iron & Ether — Session 20 (~13:43-14:36 UTC, April 1)

### Villain Design Framework (from YouTube transcript analysis)
- 4-pillar framework: Visible Evil → Overwhelming Power → Compelling Goal → Unpredictability, plus Aura
- Applied to Morgan le Fay (Aizen archetype), Oberon/Titania (World Antagonists), Mordred (sequel Griffith)
- Key design beats: Morgan "I knew you'd come here" messages at anchors, one-swat power moment, one genuine surprise (weapon bonding)
- Oberon: screen-warp presence encounter with no fight; helps Morien once for amusement
- Mordred: appears exactly once, watches, leaves
- Added to GDD as Villain Design Framework section

### Sir Dagonet — The Fraud Knight (from fraud character analysis transcript)
- Promoted from hidden NPC to major recurring character
- The King/Mr. Satan/Dan Hibiki archetype, baked into actual Arthurian legend
- Gameplay: mobile shop + map intel + Spell Fusion hints, zero combat ability
- Emotional turn in Act III: steps out to bluff/distract enemies, takes a hit for Morien
- Player arc: "useless" → "hints are cracked" → "DAGONET NO" → "favorite character"
- Added to both GDD (full design section) and Story Bible

### Three More Character Designs
- **Sir Dinadan**: cynical quest giver, ~15-20 quests, best writing in game. Mask: bone-deep loyalty he won't name
- **Sir Palamedes**: primary merchant with gated inventory that upgrades post-twist. Was sandbagging better gear. "Now you know. What will you do with it?"
- **Sir Brunor (La Cote Mal Taillée)**: hardest optional quests, best rewards. Earnest sincerity. The bloody coat. "Not yet."
- All three written into Story Bible with full profiles and key dialogue

### Expanded NPC Roster (Story Bible)
- Added 15+ new NPCs: Yvain, Gareth, Bors, Lamorak (ghost), Tristan (missable heal), Pelleas, Tor, Green Knight, Lady of the Lake (Nimue), Safir, Marhaus (dead/environmental), Caradoc (monk healer), Old King Pellinore (ghost), Dame Ragnelle, Henwen the Sow
- Character ecosystem table showing how core 4 NPCs contrast and complement

### Quest System & Spell Fusion Unlock Table (GDD)
- New design: Fusions discovered via experimentation OR quest-rewarded recipes
- Tier 3 Super Fusions: all 16 original fusions now have quest paths with specific quest givers, zones, and narrative hooks
- 6 Castlevania-inspired fusions: quest paths added
- 12 NEW fusions created to expand the grid, each with quest paths:
  - Ragnarök (Nova+Shatter+Cinder), Rhongomyniad (Lance+Stasis+Pulse), Morrígan's Flock (Volley+Bloom+Dark)
  - Mistletoe's Chains (Chain+Linger+Sap), Arondight's Shadow (Saw+Linger+Dark), Niflheim Blast (Shotgun+Stasis+Bone)
  - Green Man's Embrace (Nova+Possess+Sap), Tesla's Volley (Volley+Bounce+Pulse), Excalibur's Light (Lance+Bloom+Sap)
  - Arachne's Loom (Wave+Shatter+Silk), Camelot's Ward (Orbit+Linger+Pulse), Mordred's Trap (Seed+Stasis+Dark)
- Green Knight offers a CHOICE: Fomorian's Charge OR Chronos' Chain (can't get both from him)
- Excalibur's Light is the only story-rewarded fusion — given by Parzival in the finale, post-game only

### Ethan's Feedback
- "I love all three of these characters" (Dinadan, Palamedes, Brunor)
- "I especially love your ideas for Sir Palamedes"
- Excited about quest-rewarded Spell Fusions: "over 300 quests!"
- Wants a big NPC roster — "we can have a big roster if we do it right"
- Humorous + charismatic characters are a must
