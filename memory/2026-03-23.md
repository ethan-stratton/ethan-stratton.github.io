# 2026-03-23

## Late Night Session (continued from 03-22, ~23:00 UTC onward)

### Editor Fixes
- `39e12ed` — Floor visible in editor (brown instead of invisible gray), compact UI text with _fontSmall
- `6fb6c02` — Editor controls: Q=Tiles, E=Enemies, P=Items. Separate palettes for enemies vs items
- `ca65ab0` — Ghost platform prevention: delete now auto-saves, micro-drags under 16px cancelled, cleaned 40→14 platforms in test-arena
- `19f71d6` — X+click delete fixed (moved before drag-start), Ctrl+Z undoes entity deletions, bird lands on real surfaces
- `7c936d1` — Build fix: stray brace from old delete block
- `f6e4062` — Build fix: cast float→int for Bird.WorldToTile
- `63396b6` — Tool palette shows all 13 tools, delete shows "Nothing to delete here" feedback, Del key works

### Crawler AI Overhaul
- `c2b654a` — Complete insect behavior rewrite
  - Stop-and-go patrol with variable speed (0.6-1.4x multiplier)
  - Random pauses, direction changes, speed bursts (scurrying)
  - Edge sniffing with head bob animation
  - Startle reaction when player enters aggro range (brief freeze + squash)
  - Antennae wiggle (sine-wave visual)
  - Desynchronized start states
  - **Leaper variant**: drops off platforms ONLY during chase (surprise element), stronger jumps, orange stripe + extra legs

### Design Discussion: Mac TTS for Voice Lines
- EVE: Samantha voice + bandpass filter, chorus, slight pitch up → "through a speaker" feel
- Dragon: Albert voice + pitch down, massive reverb, granular delay → "something trying to remember language"
- Pipeline: `say -v Voice` → ffmpeg processing → game asset
- EVE progression: clean early → overtones as she self-adapts
- Dragon progression: fragments early → clearer + sadder later

### Pending
- User asked about Mac TTS voice pipeline setup — wants to use it for EVE and Dragon
- Leaper variant needs playtesting
- X+click delete: added "Nothing to delete here" feedback + Del key as alternative. May be a hit detection issue rather than input issue
- User wants crawlers to mimic a real trophic system — "make players believe these creatures have real instincts and behaviors"
- Forbidden Laboratory still DOWN (lockfile deadlock, not critical)

## ~01:30-02:05 UTC Session

### Crawler Variants Finalized
- `4b63f3c` — Fixed duplicate Crawler.cs (old class appended above new one, build error CS1513)
- `1abf5bc` — 3 crawler variants + enemy physics rewrite
  - **Forager** (default): plain brown, never aggros, ignores player, dies only when stomped from above
  - **Skitter**: greenish, thin legs + light back spot, flees at 2x chase speed for 1.5-2.5s when player approaches
  - **Leaper**: orange stripe + thick middle legs, only aggressive variant, chases/latches/jumps
  - Old `CrawlerVariant.Basic` renamed to `Forager`; `"crawler"` type in editor now maps to Forager
  - Only Leapers can latch (`CanLatch` requires `Variant == CrawlerVariant.Leaper`)
  - `ScanName` + `ScanDescription` per variant (for future scan system)
- **Enemy physics rewrite** in `EnemyPhysics.cs`:
  - Platform collision was using `velocity.Y * 0.017f + 10` (grows with speed, caused false catches)
  - Now tracks `prevFootY` (actual previous foot position) and uses "was above" check
  - Inset hitbox checks by 2px to prevent edge-clipping
  - Terminal velocity 600 cap
  - Fixes: falling through platforms, landing on wrong surfaces, glitchy jump-through

### Editor: Accordion Enemy Palette + Live Spawning
- `db60cd7` — Accordion-style enemy palette
  - Categories: Crawler ▶ (forager/skitter/leaper), Wingbeater, Bird, Dummy (dummy/crit-dummy), Swarm
  - W/S = navigate categories, Right/D = expand variants, Left/A = collapse
  - Enter/Space/Click = place, [ ] = flat cycle all variants
  - Old `EntityType` enum + flat switch replaced with `EnemyCategories` tuple array
- **Live spawning**: placing/moving/deleting enemies or items now calls `SpawnEnemiesFromLevel()` / `RespawnItemsFromLevel()` — no more "place then reload"
- `6ae3fc8` — A/D keys for accordion nav, stomp-only forager kill, better skitter flee distance

### TTS Voice Lines
- User tested Mac TTS pipeline (`say -v Samantha` → ffmpeg), said voices are "pretty annoying", deferred to later

### Commits This Block
`4b63f3c`, `1abf5bc`, `db60cd7`, `6ae3fc8`

## ~02:05-02:49 UTC Session

### Bug Fixes
- `56331db` — Unified surface edges: `FindSurfaceEdges` now merges platforms + solid tiles at same Y level (skitter was confined to 2-tile platform). Dynamic refresh every frame while grounded. Push-out-of-solid at spawn. Scan labels per variant (was all "crawler").
- `1b41d7a` — Editor off-screen enemy indicators (▼ arrow at screen edge with type label, variant colors in spawn markers)
- `3d9b120` — **Retractable spikes rendering fix**: `IsBackground()` was `(int)t >= 100` which caught `RetractSpikesLeft(110)`–`RetractHalfSpikesRight(115)`. Now excludes retractable. Editor camera unclamped (was clamped to world bounds, couldn't scroll to off-screen enemies).

### Swarm System Rewrite
- `6911543` — Leaper-as-pack-leader swarm system
  - Aggroed leaper + 3+ crawlers within 150px = swarm
  - All variants converge on PLAYER (not dummy)
  - Variant-specific swarm behavior: forager=slow/clumsy/hesitant, skitter=fast/erratic/jittery, leaper=full aggression
  - Per-crawler tick damage (leaper=5, others=2, 0.8s cooldown)
  - EVE warning on trigger + screen shake
- `e7c0dde` — **Frenzy is permanent**: once triggered, crawlers stay frenzied forever even if leaper dies. Kill leaper BEFORE it recruits, not after. Much more tense.
- **Future**: swarm targets other creatures too (priority system TBD when herbivores/predators exist)

### Task List
- Master task list exists at `genesis/game-design/master-task-list-0-30.md` — 373 tasks, 14 phases
- User wants to review/audit it next — check off what's done, prioritize what's next

### Key Lessons
- **IsBackground enum collision**: `(int)t >= 100` silently hid tiles 110-115. Always check predicate functions when adding high enum values.
- Camera clamping prevents editor from reaching out-of-bounds entities — editor needs unclamped camera.

### Commits This Block
`56331db`, `1b41d7a`, `3d9b120`, `6911543`, `e7c0dde`

### Shelter & Death System (~03:00 UTC)
- `1a7a4cf` — Shelter save system + death/respawn with consequences
  - Leaf shelters: place via item palette (P, last entry "shelter"), hold W 0.8s to rest
  - Rest: full heal, warm amber fade, auto-save shelter location
  - Death: 1.5s fade → auto-respawn at last shelter (no press R). Weather toggles on death. Death count in SaveData.
  - First death EVE: "Your vitals flatlined for... 11 seconds. The local microbiome appears to have... intervened."
  - ShelterData in LevelData. SaveData: shelterLevel/X/Y, deathCount, hp
- `cde6e45` — Fix orphaned brace breaking class scope
- `537789f` — Fix _weatherTimer (replaced with direct toggle), Player.SpriteW/H public
- `d07f377` — Fix const accessed via instance (_player.SpriteW → Player.SpriteW)
- `685efad` — Shelter movable with T+drag in editor
- `5a5ede7` — Shelter snaps to actual ground (SnapToSurface with entityH=0)

### Lessons (shelter block)
- **SnapToSurface returns entity top-left Y** (groundY - entityH). For visual markers, pass entityH=0.
- **const fields: access via type name**, not instance reference.

### Camera Zoom + Wake-Up Sequence (~03:45-04:30 UTC)
- **Camera.cs zoom system**: `Zoom`, `TargetZoom`, `ZoomLerpSpeed`, `EffectiveViewW/H`, `TransformMatrix` includes scale
- `234048d` — Camera zoom + wake-up cinematic (4 phases: blackout→eyes opening→look around→control given)
- `f2d289a` — EVE sparks + sluggish orbit during wake-up
- `88145f6` — GENESIS→GENESYS spelling fix, player grounded during wake-up (pass empty KB instead of returning early), EVE jitter slowed
- `de67bff` — EVE lissajous health scan during wake-up (vertical figure-8 pattern with scan line)
- `2c9f1d1` — Fix double `_totalTime` increment + zoom lerp conflict
- `3663fd8` — **EVE position system rewrite**: `_evePos` world position tracked every frame
  - `EveMovementMode` enum: Orbit, FlyTo, Scan
  - `EveFlyTo(target, speed, onArrive)` — reusable command for any scene
  - `UpdateEvePosition(dt, playerCenter)` called every frame
  - Smooth lerp-based following in all modes (no teleporting)
  - Wake-up: EVE spawns off-screen, flies TO Adam, scans, then transitions to orbit
- `5904ba0` — Camera zoom driven directly per frame (no lerp fighting), crosshair hidden during wake-up
  - Phase 0: hold 2.5x, Phase 1: 2.5→1.9 (ease-in), Phase 2: 1.9→1.2, Phase 3: 1.2→1.0
  - `SnapTo` called every frame during phases 0-2

### Key Decisions
- Title is **GENESYS** not GENESIS
- EVE does a **lissajous health scan** on Adam during wake-up (not random orbit)
- `EveFlyTo()` is a reusable function — EVE flies to any point from any position
- Wake-up blocks input by passing empty `KeyboardState()`, NOT by returning early (so physics/gravity still runs)
- Camera zoom during cinematic is set directly each frame (bypass lerp system) to prevent fighting/jumping

### Bugs Found & Fixed
- Double `_totalTime` increment: wake-up block incremented it, then main update did too → 2x speed jitter
- Camera lerp fighting: setting `ZoomLerpSpeed` to different values each phase caused visible jumps at transitions
- Player floated during wake-up because early return skipped `_player.Update()` (gravity)
- EVE sine frequencies too high (12f, 15f) caused seizure-like jitter → lowered to 2.5-3.5f

### Commits This Block
`234048d`, `f2d289a`, `88145f6`, `de67bff`, `2c9f1d1`, `3663fd8`, `5904ba0`

### Shelter T+Drag Snap Fix
- `0a99a6f` — Shelter re-runs `SnapToSurface` on T+drag release (was placing at grid position, not ground)

## ~04:30-05:22 UTC Session

### Build Fix + Lissajous Improvement
- `ab12989` — Missing closing brace on case 2 (build error CS1003/CS1513). Fuller lissajous: 18×36px, 0.8x speed, π/4 phase shift for tilted figure-8.

### Camera Snap + EVE Spawn Fix
- `f83bc4e` — Phase 3 zoom now runs SnapTo every frame, interpolates to 1.0 over full 2s (no instant Zoom=1f jump). EVE spawns at visible screen edge at current zoom level (not high in air).

### Extended Scan Phase
- `f04e9c7` — Phase 2 doubled from 4s to 8s for more lissajous scanning time.

### Adam Lying Down → Standing Up Animation
- `e318694` — `Player.IsLyingDown`, `StandUpProgress` (0=flat, 1=standing), `BeginStandUp()`, `UpdateStandUp()`
  - 1.5s stand-up with quadratic ease-out
  - DrawFallback: horizontal body morphs to vertical, head shifts side→top, arm pushes off ground mid-stand
  - Bottom-anchored (feet stay planted)
  - Phases 0-1: lying flat. Phase 2 at 4s mark: starts standing. Phase 3: fully standing.

### EVE Velocity-Based Physics
- `d00f187` — Replaced lerp/snap with proper physics: `_eveVel`, acceleration toward target, drag damping
  - EveMaxSpeed=350, EveAccel=600, EveDrag=3.5, per-mode tuning
  - Distance-based speed: 0.3x close, up to 3x far. More drag near Adam, less far.
  - FlyTo decelerates near target. Arrival requires proximity AND low velocity.
  - Both orbit and scan now rotate clockwise (fixed direction mismatch).

### EVE Speed + Triangulation Scan
- `d21233c` — Faster EVE (350 max, 600 accel, distance-based multiplier up to 3x). Triangulation scan: 3 dashed lines from EVE to 3 points on Adam, sweep up/down, triangle outline + pulsing dot.

### EVE Save Persistence + Prologue Skip + Shared Triangulation
- `f3f805e` — Three changes:
  1. EVE state saved when wake-up completes (was lost on quit)
  2. Skip prologue (hold Escape) → no wake-up, no EVE. Player must find her manually. Full cutscene → EVE automatically.
  3. `DrawTriangulationScan(anchor, target, time, alpha)` — shared method for both enemy scan and wake-up scan. Semi-transparent scanline fill inside triangle. Replaced old single green beam for enemy scans too.
- `9a97265` — Refined: proper morphing triangle with 2 independently orbiting vertices on target, anchor at EVE. Filled triangle using scanline rasterization (every-other-row for CRT look).

### Reference Repo Comparison Doc
- Wrote `arena-shooter/game-design/reference-comparison.md`
- **Stardew save vs ours**: We need save versioning, settings separation, structured world state
- **Stardew dialogue vs ours**: We need external dialogue file, click-to-advance box, emotion tags
- **AM2R transitions vs ours**: We need player walk-through during fade, camera bounds per room, transition types
- **AM2R enemy AI vs ours**: Our crawler BugState is MORE nuanced for ambient behavior, but we have ZERO combat reaction (no hitstun, stagger, dodge). Need EnemyState base class.
- **AM2R gating vs ours**: Tier gating designed but not implemented. Unique: Dragon attention, tech suppression, knowledge gating.
- Priority for first 30 min: save versioning, room transitions, dialogue file, enemy hitstun

### Commits This Block
`ab12989`, `f83bc4e`, `f04e9c7`, `e318694`, `d00f187`, `d21233c`, `f3f805e`, `9a97265`

### Pending
- User wants to review first-30-minutes task list next and pick 1-2 more things to do tonight
- Forbidden Laboratory still down (lockfile deadlock, restarted but likely fails again)

### Combat Knife (~05:30 UTC)
- `4511362` — `WeaponType.Knife` added (SF6 straight jab, 28×12 hitbox, 8dmg/12 finisher, fastest weapon)
- `93b8317` — Fix variable name collision `t`→`kt` in knife hitbox (CS0136)

### Grapple Gun (~05:40 UTC)
- `7fe3e68` — Full pendulum swing physics. Fire E, reel W/S, release Space/E
- Constants: GrappleGravity=600, GrappleDamping=0.98, GrappleReleaseBoost=1.2, max 200px
- "grapple" item in editor palette. HasGrapple persists via CollectedItems

### Reference Comparison Doc
- Wrote `genesis/game-design/reference-comparison.md` (Stardew save/dialogue, AM2R transitions/AI, Celeste physics)
- Save needs: version field, settings separation. Dialogue needs emotion tags + branching

### Task List Updated
- `1c35b2d` — 30 done, 8 partial, 338 remaining across 14 phases

## ~14:44-15:30 UTC Session (Morning for Ethan, ~8:44 AM MDT)

### Grapple Gun Rewrite — Three Commits
- `e9f6b97` — First pass: raycasted hook (8px steps), anchor-then-activate flow, scan→Q keybind, knife+grapple in debug spawn menu (P key)
- `b79f88e` — **Complete physics rewrite**: threw out angular pendulum, replaced with Cartesian constraint system
  - Gravity on velocity + rope constraint (snap to circle, strip outward radial velocity)
  - Fixes "always swings left" bug (angular math was losing velocity sign)
  - Auto-attach on contact (ESA-style, no anchor-wait step)
  - Hook retracts on miss instead of anchoring in air
  - `PlayerWeight` placeholder added (1.0 base, multiplies grapple gravity)
  - Release on E-release or Space
- `a6dae28` — **Critical bug: GetTile() double-division**. Was passing `(int)(sp.X/32)` to `GetTile()` which internally divides by TileSize again. Hook could never hit tiles. Fixed by passing raw world coords.

### Key Decisions
- Scan keybind: E→Q (E reserved for grapple + future equippable tools)
- Grapple physics: Cartesian pendulum (gravity + rope constraint) instead of angular math
- Auto-swing on contact (ESA-style), no two-step anchor
- Hook miss = retract (fail), NOT anchor in air
- `PlayerWeight` stat added as placeholder for future equipment weight system
- Player's falling speed carries into swing momentum
- Knife + Grapple added to debug spawn menu

### Environmental Station Alpha Reference
- ESA hookshot: fixed 45° angle, instant swing on contact, short range (~3 tiles), momentum preservation
- Fast/snappy — designed for chaining rapid swings (Bionic Commando feel)
- Our approach: mouse-aimed (more flexible), longer range, same instant-swing principle

### Ethan's Grapple Feature Requests
- Trajectory affected by fall speed ✅ (velocity carries into swing)
- Material-based grapple behavior (future)
- No "press E again" — auto-swing ✅
- Enemy grapple: pull self to large enemies, pull small ones to player (weight-based)
- PlayerWeight stat affects grapple trajectory ✅ (placeholder)
- Rope/wall auto-latch to stay as-is

### Bugs Found
- **GetTile double-division**: `GetTile()` takes world pixel coords, not tile indices. Passing pre-divided col/row meant it always checked near origin. **LESSON: always check whether API expects world coords or tile coords.**
- **Angular pendulum always swung left**: `atan2` → angular velocity conversion lost horizontal velocity sign relative to anchor. Cartesian approach eliminates this entire class of bug.
- **_prevKb set before grapple input runs**: Space-release detection broken because `_prevKb = kb` happens at line 1495 but grapple input is at line 1912. Minor issue, not yet fixed.

### Forbidden Laboratory
- Still down (lockfile deadlock). Restarted during heartbeat check.

### Commits This Block
`e9f6b97`, `b79f88e`, `a6dae28`

### Pending
- Ethan testing grapple (`a6dae28`) — awaiting feedback on whether hook now attaches to tiles
- Terrain collision while swinging (player clips through walls) — not yet implemented
- Enemy grapple (weight-based pull direction) — not yet implemented
- Material-based grapple behavior — future

## Afternoon Session (~15:00–19:00 UTC)

### Grapple Gun Bug Blitz
- **`a6dae28`**: Grapple hook collision was double-dividing coordinates — `GetTile()` takes world coords but we pre-divided by 32. Hook never hit anything.
- **`4f75008`**: Momentum preserved on release (old `Velocity = vel` nuked grapple vel), retract animation added, wall/floor entity collision, background tiles ignored (uses `IsSolid()`)
- **`efc2825`**: Fixed normal movement broken by grapple fix — `vel = Velocity` was running every frame, overwriting gravity/input. Added `_grappleJustReleased` one-frame flag.
- **`70beb14`**: Reel teleport fix (stop reeling at 20px from anchor), hook gravity for downward shots (0.5x GrappleGravityBase on Y velocity)
- **`5c975a2`**: Terrain collision during swing = push-out + bounce (not auto-release). Momentum boost: Gravity 900→1100, Damping 0.8→0.3, ReleaseBoost 1.1→1.15, InputForce 400→500
- **`f22d037`**: Changed from auto-release on terrain contact to AABB push-out + 50% velocity bounce. Player keeps control.
- **Design decision**: Auto-release on terrain was wrong — removes player agency. Push-out + bounce is correct.

### Editor & Knife Fixes
- **`74c2936`**: Items (knives, grapples) now deletable in editor. `TryDeleteAt()` was missing item check entirely.
- **`eb09ea9`**: Knife direction fixed (was using AimDir/mouse, now uses FacingDir). Three stances: standing stab, crouch stab (8px lower), angled downward (crouch + S + A/D = 45° diagonal).

### Inventory
- Tab menu now shows TOOLS section with grapple gun status
- HasGrapple persists across level loads (confirmed via CollectedItems save)

### Grapple Constants (current)
- `GrappleGravityBase=1100f`, `GrappleDampingRate=0.3f`, `GrappleMinLength=24f`
- `GrappleReleaseBoost=1.15f`, `GrappleInputForce=500f`, `GrappleHookSpeed=800f`
- Hook projectile has gravity (accelerates downward shots)
- `GrappleNudgeVel()` method added for terrain bounce during swing

### Next Up: Enemy Grappling
- User wants: large enemies pull player TO them, small enemies pulled TO player
- Weight-based system proposed: Crawler 0.2, Bird 0.3, Wingbeater 1.5, Dummy 1.0
- NOT yet implemented — awaiting user go-ahead

---

## Evening Session (~19:00–21:00 UTC)

### Bombardier Crawler Variant
- Implemented in `06657ca`: ranged defender inspired by real bombardier beetles
- Faces AWAY from player, sprays hot particles backward (real beetle behavior)
- Predictive aiming: `predictedPos = playerPos + playerVelocity * 0.3f`
- 0.5s telegraph with orange body glow + pulsing abdomen
- 8 particles in ±25° fan, 200–350px/s, 1 damage each, 3s cooldown, 150px range
- Build fix `5e9d752`: `CurrentHp`→`Hp`, removed nonexistent `HitFlash` on Player, `playerRect2`→`_player.CollisionRect`
- Enemy design research saved to `game-design/video-research-2026-03-23-enemies.md`

### Major Design Pivot: Rain World-Style Procedural Animation + Ecosystem AI
- Ethan watched 4 ecosystem/worldbuilding videos and proposed shifting away from sprite-based animation
- Videos: Red Dead ecosystems, Planet of Lana, worldbuilding theory, Rain World dev history
- **Key decision**: Move to procedural animation (interconnected limbs with IK) instead of flipbook sprites
- Ethan's reasoning: he's not a great sprite artist; procedural trades art for code
- **AI philosophy shift**: "How can this creature find food and get home before night?" instead of "How can this enemy be a good obstacle?"
- Rain World principles adopted: off-screen simulation, sound-based detection, dynamic relationships, personality per creature, lineage system

### Procedural IK Legs — First Implementation (`960d975`)
- `IKLeg` struct: 2-segment inverse kinematics using law of cosines
- 6 legs per crawler, tripod gait (alternating groups 0,2,4 and 1,3,5)
- Smooth step animation with arc (smoothstep lerp + sine lift, 0.08s per step)
- Segmented body: head, thorax, abdomen with movement bob
- Variant-specific limb proportions: Skitter=long, Leaper=powerful rear, Bombardier=stubby
- Eyes change color: pale when calm, red when aggroed
- `DrawLine()` helper for rotated rectangle rendering
- `UpdateLegs(dt)` called every frame after physics

### Saved Documents
- `game-design/video-research-2026-03-23-ecosystems.md` — full research notes from all 4 videos
- `game-design/procedural-animation-plan.md` — 6-phase implementation plan:
  1. Crawler legs (DONE — basic)
  2. Sound-based detection
  3. Goal-based AI (needs/food/nests)
  4. Dynamic relationships (Rain World social memory model)
  5. Player procedural animation
  6. All creatures procedural

### Commits
- `06657ca` — Bombardier variant + enemy research notes
- `5e9d752` — Bombardier spray damage build fix
- `558cfbb` — Enemy grappling (pull small, yank wingbeaters)
- `960d975` — Procedural IK legs for crawlers

### Forbidden Laboratory
- Still down (lockfile deadlock, os error 35). Not critical.

### Evening Session — Creature Architecture Refactor

**IK Leg Fixes:**
- `45ba458` — Legs 2x bigger, thicker rendering (3px/2px), wider spread, dangle when latched/airborne
- `5476fe1` — UpdateLegs called before latch early return so legs actually dangle. Foot positions clamped to max reach (fixes stretching glitch on leapers)
- Ethan tested: legs are small but work well enough. Leaper legs were glitchy/stuck. Fixed.
- Ethan noted fishing as a future feature — added to mental notes.

**Creature Base Class Refactor:**
- `3e8087e` — Created `Creature.cs` abstract base class. Crawler, Bird, Wingbeater all inherit.
  - Shared: Position, Velocity, Hp, MaxHp, Alive, HitFlash, VisualScale, Dir
  - New: `Guid Id` (persistent identity), `CreatureNeeds` (Hunger/Fatigue/Safety), `EcologicalRole` enum
  - New: `CurrentNodeId`, `GoalNodeId`, `HomeNodeId` (world graph for off-screen sim)
  - `EcologicalRole`: Herbivore, Predator, Scavenger, Decomposer, Apex, Prey, Defensive, Flighty
  - `ApplyVariantRole()` on Crawler syncs Role from Variant
- `8b6db0f` — Extended to Hopper + Thornback. Added unified `List<Creature> _creatures` in Game1.
  - All 5 enemy types inherit Creature. All adds/clears/removes synced.
  - Typed lists (`_crawlers`, `_birds`, etc.) kept alongside for 59 existing type-specific callsites.

**Off-Screen Simulation Design:**
- Discussed Rain World-style persistent creature simulation
- Creatures simulate always (simplified off-screen), render only on-screen
- Architecture foundation documented in `procedural-animation-plan.md` (`7585a3a`)
- Priority: (1) Creature base ✅, (2) CreatureNeeds ✅, (3) Persistent IDs ✅, (4) WorldNode graph SOON

**Key Design Decisions:**
- `EcologicalRole` as data + virtual overrides for species-specific behavior (both, not either/or)
- `TrophicTier` int proposed for predator hierarchy (tier 3 > tier 2 > tier 1, drives competition)
- Predators CAN fight predators for kills; higher-tier predators intimidate/kill lower-tier ones
- Same-species competition at food sources (hungrier one gets priority or they fight)
- ~35 of the 59 typed callsites COULD be collapsed to polymorphic calls now (draw, damage, death removal) — held off to avoid double-refactor risk. Ethan asked about this; offered to do a second pass.

**Pending from Ethan:**
- Has YouTube videos to watch and record (queued, hasn't sent yet)
- Wants the 35 easy callsites collapsed (implicit approval, waiting on explicit go-ahead vs videos first)

**Forbidden Laboratory:** Still down. Lockfile deadlock persists across restarts.
