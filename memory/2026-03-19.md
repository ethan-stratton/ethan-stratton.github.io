# 2026-03-19

## Late Night Session (~03:00-03:16 UTC)

### Bird Entity Added (commit `ae238c1` area)
- New `Bird.cs` — ambient wildlife, non-hostile
- States: Perched → Pecking → Hopping → Fleeing → Flying
- 10×8 brown sparrow, flees at 140px, takes off at 80px
- Added to editor as placeable entity type
- Wired into Game1: list, spawn, update, draw

### World Map Zoom (commit `94b8e6e`)
- Q zooms out, E zooms in (0.25x to 2x)
- Everything scales with zoom level

### Full Wilbur-Pipeline World Map Rewrite (commit `980cf91`)
- Map changed to 60×100 (vertical continent)
- 35 tile types (up from ~17): DeepOcean, ShallowOcean, Reef, Beach, RockyShore, Grassland, Savanna, Jungle, Hills, Foothills, HighMountain, Glacier, Taiga, SnowForest, FrozenLake, Dunes, Wasteland, Marsh, Floodplain, River, Lake, Cave, Ruins, Oasis + originals
- 15-stage generation pipeline:
  1. Fractal noise (4 layers: height 7oct, moisture 5oct, temp 3oct, ridge 5oct)
  2. Gaussian island shaping (Wilbur formula: curval×exp(-sqr(r/k))-sqr(r))
  3. Mountain ridge injection
  4. Basin filling (Planchon-Darboux) — 2 passes with noise between
  5. Water flux accumulation (sort descending, cascade rainfall)
  6. Incise flow erosion (slope × sqrt(flux) × 0.015, capped)
  7. Thermal erosion (4 passes)
  8. Coastline smoothing (3-pass majority-neighbor filter)
  9. Biome assignment (height × temp × moisture, 6 elevation bands)
  10. Rivers from flux network (threshold 25+)
  11. Lakes at local minima with high flux
  12. Desert detail (dunes from ridge noise, oases near water)
  13. Caves at mountain bases
  14. Ruins in dense forest/jungle/wasteland
  15. Volcano (one per map, hot zone high mountains)
- Seed 42, deterministic. Plan: iterate until map looks good, freeze it, eventually remove generation code.
- Build error fix: extra `}` in GetWorldTileColor from python line replacement

### Research Done
- Read Wilbur "Fun With Wilbur" Vol 1, 3, 6 tutorials from Wayback Machine
- Read mewo2's fantasy map generator notes (the @unchartedatlas bot)
- Key Wilbur techniques: basin filling (Planchon-Darboux 2001), incise flow, precipiton erosion, Gaussian island shaping with cubic modifier
- Key mewo2 techniques: irregular Voronoi grids, water flux accumulation, erosion = slope × sqrt(flux), coastline majority-neighbor smoothing

### Design Decision
- World map is NOT roguelike — generate once, find a good seed, save it permanently. Levels will be hand-crafted. Generation code may be removed later.

### Enum Values Changed
- MapTileType enum completely renumbered (DeepOcean=0, Ocean=1, ShallowOcean=2, etc.)
- Old worldmap.json deleted to force regeneration
- Walkability updated: Ocean/DeepOcean/ShallowOcean/Reef/HighMountain/Volcano/Lake/FrozenLake/Glacier all unwalkable

## Worldbuilding Framework (from "Seven Levels of Worldbuilding" video discussion)

Ethan asked to remember this deeply. The framework for Genesis worldbuilding:

**Target: Level 5 with selective Level 6.**

### The Five-Point Plan (Ethan said "this is important"):
1. **Start with the creation myth** — write it as conflicting accounts (faction A says X, faction B says Y). Gives Level 5 for free.
2. **Pick ONE core power/technology constraint** (Level 4) — the "no thinking machines" of our world. One rule that cascades into combat, society, architecture.
3. **Build the map AFTER the history** (Level 1) — don't draw land then justify it. Know what happened, let geography reflect it.
4. **Let Rule of Cool live inside constraints** (Level 2→4/5) — one sentence of justification tied to history/power rules turns cool ideas into deep worldbuilding.
5. **Surface identity comes from gameplay** (Level 3) — each region should feel different to PLAY, not just look different. Visual identity follows mechanical identity.

### Key Principle:
"Write the myth first, derive everything else from it." Genesis source material already has the deep history and cultural weight baked in.

## Combat Overhaul Session (~04:00-05:00 UTC)

### Combo Chain System Implemented
- **Fist combo** (WeaponType.None): 2-hit SOTN-style jab chain, narrow 18×20 hitboxes, 0.06s active each, 0.35s combo window
- **Stick combo** (WeaponType.Stick): 3-hit chain — forward sweep → extended sweep → conditional overhead slam
  - Hit 3 only comes out if hits 1 and 2 both connected
  - Hit 3 reworked to 3-phase cascading slam (top→mid→low, 0.06s each phase, 0.18s total)
  - Each phase hits independently with its own hitstop (Dragon's Dogma style multi-hit)
  - 2 damage per phase hit (6 total if all connect)
- MeleeHitbox now uses live AimDir (not frozen FacingDir from input time) — fixes crouch+turn hitbox bug
- Stick combo tilts up/down with held direction for directional aiming

### Hit Feedback System
- Red flash (0.15s) on all enemies on hit (Crawler, Hopper, Thornback)
- MeleeHitCooldown (0.2s) added to all enemies — prevents multi-hit per frame, was causing screen shake to be invisible
- Hitstop: 0.03s on hit, 0.05s on finisher phase, 0.06s on kill — freezes entity updates, draw continues
- Screen shake: 5px/0.1s on hit, 6px/0.12s on finisher, 8px/0.15s on kill

### Slide Hitbox
- Collision now uses CurrentHeight (24px) during slide — ceilings, walls, solid floors all respect shorter hitbox
- playerRect2 in Game1 uses _player.CurrentHeight with proper Y offset
- Can actually slide under objects now ✅

### Uppercut L-Shape (Richter SOTN style)
- Two rectangles: vertical arm (16×28 above head) + horizontal arm (24×20 forward at chest)
- UppercutHitboxes returns Rectangle[], old UppercutHitbox returns Union for compat

### Spawn Item Menu
- P key opens spawn menu (editor mode only)
- Spawns: Stick, Sword, Axe, Gun, Bow, Sling
- Items pop above player with gravity, land on platforms now (fixed fall-through bug)

### Bugs Fixed
- P menu was opening+closing same frame (consumed keypress with early return)
- Melee hitting every frame (added MeleeHitCooldown to all enemy types)
- Screen shake invisible at 2px (bumped to 5/6/8px)
- Fist hitbox stuck on right side when crouching left (now uses live AimDir.X)
- Stick hit 1 was behind player (all hits now swing forward in facing direction)
- Items falling through platforms (added platform collision to item physics)

### Design Docs Updated
- STORY-BIBLE.md: Added "The Three Accounts" — Federation Record, Natives' Oral History, Player-Pieced Truth
- GDD.md: Added Core Constraint ("The Planet Resists Technology"), Map Philosophy (history-first), Rule of Cool Inside Constraints (with Babel Hive/Floodwalker/Watcher justifications), Biome Design table (Eden Reach, Petrified Forest, Ashlands, Coral Heights, The Depths, Void Rift)
- Ethan said the worldbuilding output was "all gold" — add to design doc immediately

### Azgaar's Fantasy Map Generator
- Ethan asked about using it for Genesis map
- I can reach the page but can't interact (JS app). Suggested: generate a map, export JSON, share it for parsing
- Discussion ongoing

### Key Quotes/Decisions
- "The planet resists technology" is the ONE core constraint that cascades into everything
- The Dragon being cybernetic breaks the planet's rule — that's part of the mystery
- Biomes should feel different to PLAY, not just look different
- Ethan wants Level 5 worldbuilding with selective Level 6

## Session ~05:08-05:45 UTC

### Level File Corruption & Fix
- My python scripts to edit exit data in level JSONs (commit `3d23e5e`) truncated test-arena (24K→11K) and the-descent (25K→11K) — lost tile data
- Restored originals from commit `b8aaf54`, re-applied exit changes carefully (commit `0e6609f`)
- debug-room exit-left → `__overworld__`, the-descent exit-left → `__overworld__`
- **Lesson:** Python json.dump compacts + can drop empty arrays. Always verify file sizes after programmatic JSON edits.

### Player Sprite Sheet (commit `dc86fa1`)
- Generated 12-frame 32×48 sprite sheet via Node.js + ImageMagick (`Content/sprites/adam_sheet.png`)
- Frames: idle, run1-4, jump, fall, slide, crouch, attack1 (fist), attack2 (stick overhead), attack3 (stick slam)
- Design: blue-grey Federation compression suit, torn left shoulder, cracked cyan visor, dark hair, brown utility belt, dark boots
- Loaded via `Texture2D.FromStream` in Game1, passed to `Player.Draw(spriteBatch, pixel, spriteSheet)`
- Player.Draw rewritten: `GetSpriteFrame()` + horizontal flip, fallback `DrawFallback()`
- `_animTimer` for run cycle (4 frames at 10fps)

### CRT Shader (commit `dc86fa1`)
- HLSL shader at `Content/shaders/CRT.fx` — scanlines, barrel distortion, chromatic aberration, phosphor dots, vignette
- Software fallback: scanline overlay texture (1×4px tiled) + vignette rects — works without compilation
- Toggle: Settings → Graphics → "CRT Filter" (`_crtEnabled`)
- All draw paths route through `DrawCRT()` before `base.Draw()`
- `RenderTarget2D _crtTarget` recreated on window resize
- Compile full shader: `mgcb /build:Content/shaders/CRT.fx /outputDir:Content`

### CollisionWidth (commits `2846f68`, `dc86fa1`)
- `Player.CollisionWidth = 21`, `CollisionRect` property for damage checks
- Movement collision stays at full `Width = 32`
- Item pickups/exits use full width (generous)

### Confirmed by Ethan
- I-frame flashing works — no need to verify
- Knockback works — no need to verify
- Ceiling slope bugs: leave alone, not priority
- Wants CRT shader, wants sprites in-game

## Afternoon Session (~13:21-13:58 UTC)

### Richter Sprite Sheet Implementation
- Ethan dropped a Richter Belmont (Rondo of Blood) sprite sheet (`Content/sprites/15222.png`)
- Sheet is 474x896, irregular layout, green chroma key background (`srgb(128,255,128)`)
- Extracting frames was painful — tightly packed rows with no clean separators, sprites bleeding between rows
- Multiple approaches tried: connected components (split body parts), auto-detect (merged multi-frame blobs), row bands (overlap issues)
- Final approach: manual per-frame bounding boxes with Node.js script, `-gravity South -extent 48x48` for bottom-alignment
- Sheet rebuilt as 480x480 (10 cols x 10 rows, 48x48 per frame)
- Row layout: 0=idle(6), 1=crouch(5), 2=jump(8), 3=walk(7), 4=run(8), 5=whip(10), 6=backflip(10), 7=damaged(3), 8=superjump(6), 9=dash(3)
- Player.cs updated: `GetSpriteFrame()` returns `(row, frame)` tuple, `SpriteW=48, SpriteH=48`
- Draw: bottom-aligned, centered on 32px collision box, horizontal flip via `SpriteEffects`
- Commits: `5be74b9` (initial wiring), `6e5453c` (corrected Y crops)

### Pause Feature
- Added `_isPaused` toggle on B key during `GameState.Playing`
- Draws pause symbol (‖) bottom-right + "PAUSED" center text
- All game updates freeze when paused — designed for screenshot debugging
- Commit: `de0fb8d`

### Open Bug: Sprite Rendering
- Ethan reports "feet above head" in jump, run, crouch animations
- Sheet frames verified correct (feet at bottom in PNG) — issue may be in-game coordinate mismatch or stale file
- Still investigating — awaiting screenshot from Ethan

### Token Usage
- At 88% context (112k/128k), 100% Premium + Chat remaining on GitHub Copilot Claude Opus 4.6

## Afternoon/Evening Session (~14:00-20:14 UTC)

### Sprite Sheet Rebuild v3-v9 — Center-Based Cropping
- Fundamental problem: source sprites are irregularly spaced, some overlap horizontally
- Switched from uniform grid to per-sprite center X coordinates with individual crop widths
- Multiple iterations fixing bleed from neighboring sprites (floating fists, hands, text labels)
- Key technique: measure exact sprite centers via column-scanning, crop tight, shift centers to avoid neighbors

### Sprite Sheet Final Layout (as of v9, commit `b0e748c`)
- Row 0: idle (6 frames) — Stand A (0-3) + Stand B (4-5), frame 4 uses 14px tight crop
- Row 1: crouch (3 frames, 20px crop)
- Row 2: jump (8 frames, 22px crop)
- Row 3: walk (8 frames, 20px crop) — frame 6 center shifted left to 158 to avoid hand bleed
- Row 4: run (8 frames, 28px crop, spriteH=44 to avoid "Walk" text bleed)
- Row 5: whip (10), Row 6: backflip (10), Row 7: damaged (3), Row 8: superjump (6), Row 9: dash (3)

### Animation State Machine Rewritten
- Idle: Stand A (frames 0-3) loops 3x at 3fps (~4s), then Stand B (frames 4-5) plays once and holds frame 5
- Crouch: 3-frame transition (forward to crouch, reverse to stand), with `_crouchTransTimer` and `_crouchTransDown`
- Walk vs Run: regular movement (A/D) → Walk row; dash (double-tap) → Run row
- Blade dash → Dash row (row 9)
- Jump up: frame 1; Fall: frame 3 (clean descending pose, not frame 5 which was a kick artifact)
- `Player.Paused` property added — freezes `_animTimer`, `_idleTimer`, `_crouchTransTimer`

### P Spawn Menu Bug Fixed
- Was inside `UpdateEditor()` which only runs in `GameState.Editing`
- Moved P key handling + menu navigation to Playing state (after pause check)
- Commit: `f330093`

### Stand B Pixel Tuning
- Ethan requested Stand B frames shifted left — done in 2 increments (total -2px from original centers)
- Frame 4: cx 109→107, Frame 5: cx 128→126
- Commits: `f330093`, `b0e748c`

### Key Commits This Session
- `2eb717d` — Sprite v4: full feet, crouch transition anim, idle Stand A/B
- `fa2d9cf` — Sprite v6: fix crouch fist, walk hand, run text bleed, pause freezes anim, walk/run speed split
- `1305588` — Idle 6 frames with tight crop on frame 4
- `f330093` — P menu fix + Stand B shift
- `b0e748c` — Stand B shifted 1 more px

### Design Philosophy Doc Created
- `game-design/design-philosophy.md` synthesizing 3 YouTube video analyses
- 7 principles: Silhouette Is King, Core/Peripheral/Indulgent worldbuilding, Mecha Dragon as tragic villain, Mystery as design principle, Freedom within archetypes, Visual hierarchy, POV as worldbuilding vehicle
- Commit: `368eca8`

### YouTube Videos Researched (via youtube-full skill)
1. "7 Things You're Doing Right in Worldbuilding" — Core/Peripheral/Indulgent framework
2. "Modern Game Developers Forgot How to Make Characters" — TF2 silhouette-first, high skill ceiling
3. "How to Write a Tragic Villain" — Want vs Need inversion, mirror the hero

### Pending
- Ethan testing Stand B after latest 1px shift
- Walk animation may still have minor boot flicker (inherent sprite height variance)
- Lower rows (whip, backflip, superjump) not yet cleaned up — focus on top 5 rows first
- Next: other animation polish, enemy encounters, sword/axe combos

## Evening Session (~20:30-21:30 UTC)

### Video Research Batch 3
Fetched transcripts for 3 YouTube videos Ethan requested:
1. **"The Hand Drawn Graphics Of Silksong" (Acerola)** — 5 pillars: depth of field, parallax, particles, post-processing, hand-painted art. Key tech: Interleaved Gradient Noise (IGN) = one-line shader fix for color banding. Pixel-perfect parallax mandatory for pixel art games.
2. **"Gyms, Zoos, and Museums" (Robin-Yann Storm)** — In-game documentation pattern. Gym=movement tests, Zoo=asset display, Museum=system demos. Spatial documentation > written docs that go stale.
3. **"How to make games look GOOD" (Giant Sloth Games, short)** — LUT color grading: neutral LUT + Photoshop adjustments → load in engine for real-time color grading at zero cost.

Research saved to `game-design/video-research-2026-03-19-c.md` (commit `4313fb1`).

**Ethan's directives:**
- DO NOT FORGET graphics improvements from video 1 (IGN, parallax, particles, bloom, depth of field) — implement later
- DO NOT FORGET LUT color grading — implement later
- Implement Gyms/Zoos/Museums pattern NOW

### Sprite Sheet Work Abandoned (for now)
- Multiple rounds of ImageMagick sprite extraction from `15222.png` kept producing artifacts
- Fixed `BlendState.NonPremultiplied` for transparency in MonoGame
- Ethan deleted generated sheets, kept original `15222.png`
- **Decision**: Shelve sprite extraction. Needs proper pixel art tool, not ImageMagick surgery.

### Debug Room Gym Built (commit `9d7af35`)
- Expanded debug room from 80×13 to 160×25 tiles
- **Jump Gym**: platforms at 4/6/7/10 tile gaps, color coded
- **Height Gym**: walls at 1/2/3/4 tiles (jump peak = 98px = 3.1 tiles)
- **Tunnel Gym**: 2-tile (walk) and 1-tile (crouch)
- **Enemy Zoo**: 5 walled pens — crawler, hopper, thornback, swarm, bird
- **Slope Section**: 45° and gentle slopes
- **Spatial Labels**: 26 labels with metrics, added `LabelData` to `LevelData.cs`
- Labels render in both editor and gameplay modes

### Graphics/LUT Notes Added to MEMORY.md
- Added "MUST IMPLEMENT" sections per Ethan's explicit instruction

## Late Evening Session (~22:36-23:18 UTC)

### Enemy Physics Overhaul (commits `e2171a0`, `7e558c9`)
- Created `EnemyPhysics.cs` — shared static helper class for all enemy physics
  - `ApplyGravityAndCollision()`: gravity + tile/platform/floor collision
  - `FindSurfaceEdges()`: tile-grid-aware surface edge detection for patrol bounds
  - `SnapToSurface()`: tile-aware spawn snapping (replaces old Game1 method)
- Refactored Crawler, Hopper, Bird to use EnemyPhysics (all receive tileGrid in Update)
- SpawnEnemiesFromLevel now uses EnemyPhysics.SnapToSurface
- Root bug: old `SnapToSurface` and `FindSurfaceEdges` in Game1 only checked platforms/solidFloors/walls, completely ignoring tile grid

### Variable Jump Height (commit `a444251`)
- `JumpCutMultiplier = 0.4f` — releasing Space mid-air cuts upward velocity to 40%
- Fires only on exact frame Space is released (tracks `wasHoldingJump`)
- Excluded from flip, uppercut, vault kick (fixed trajectories)
- Ethan confirmed: "the jump physics works awesome"

### Slide/Crouch Headroom Checks (commit `a444251`)
- `HasHeadroom()` method checks ceilings, solidFloors, and tile grid for clearance
- Slide end: can't stand → force crouch; can't crouch → extend slide (recheck 0.05s)
- Crouch release: only uncrouch if headroom for full Height
- Prevents getting stuck in low tunnels

### Critical Collision Bug Fix (commit `d609527`)
- **Root cause**: ALL solid floor, ceiling, and wall collision used full sprite `Width` (32px) instead of `CollisionWidth` (21px)
- Solid floor horizontal push-out also used `pos.Y` instead of `pos.Y + slideOffset` for collision top during slide
- This caused: slide tunnel impassable (pushed out sideways by ceiling blocks), can't fall through 1-tile gaps (32px player vs 32px gap = 0 clearance)
- Fix: ceiling, solid floor, and wall collision all now use CollisionOffsetX/CollisionWidth consistently

### Debug Room Tunnel Gym Rebuilt (commits `3199a21`, etc.)
- Smooth transition: 12-tile open runway → walk tunnel (64px, rows 21-23) → slide tunnel (32px, row 22-23)
- Previous versions had walls between sections and zero-gap ceiling placement

### Pending
- Ethan hasn't tested the collision fix yet (slide tunnel + 1-tile gaps)
- Enemy zoo pens rebuilt at cols 110-155
- Sword/Axe movesets still future priority
- Ethan mentioned having "ideas for what we can do next" after physics fix
